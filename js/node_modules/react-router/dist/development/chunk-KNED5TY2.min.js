import { r as p } from "../../../../_virtual/index.min.js";
import "../../../../_virtual/index.min3.js";
/**
 * react-router v7.5.0
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */
var br = (e) => {
  throw TypeError(e);
}, ia = (e, t, r) => t.has(e) || br("Cannot " + r), bt = (e, t, r) => (ia(e, t, "read from private field"), r ? r.call(e) : t.get(e)), la = (e, t, r) => t.has(e) ? br("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(e) : t.set(e, r), Xt = "popstate";
function sa(e = {}) {
  function t(n, a) {
    let { pathname: o, search: l, hash: s } = n.location;
    return Ke(
      "",
      { pathname: o, search: l, hash: s },
      // state defaults to `null` because `window.history.state` does
      a.state && a.state.usr || null,
      a.state && a.state.key || "default"
    );
  }
  function r(n, a) {
    return typeof a == "string" ? a : ve(a);
  }
  return ca(
    t,
    r,
    null,
    e
  );
}
function z(e, t) {
  if (e === !1 || e === null || typeof e > "u")
    throw new Error(t);
}
function K(e, t) {
  if (!e) {
    typeof console < "u" && console.warn(t);
    try {
      throw new Error(t);
    } catch {
    }
  }
}
function ua() {
  return Math.random().toString(36).substring(2, 10);
}
function qt(e, t) {
  return {
    usr: e.state,
    key: e.key,
    idx: t
  };
}
function Ke(e, t, r = null, n) {
  return {
    pathname: typeof e == "string" ? e : e.pathname,
    search: "",
    hash: "",
    ...typeof t == "string" ? ye(t) : t,
    state: r,
    // TODO: This could be cleaned up.  push/replace should probably just take
    // full Locations now and avoid the need to run through this flow at all
    // But that's a pretty big refactor to the current test suite so going to
    // keep as is for the time being and just let any incoming keys take precedence
    key: t && t.key || n || ua()
  };
}
function ve({
  pathname: e = "/",
  search: t = "",
  hash: r = ""
}) {
  return t && t !== "?" && (e += t.charAt(0) === "?" ? t : "?" + t), r && r !== "#" && (e += r.charAt(0) === "#" ? r : "#" + r), e;
}
function ye(e) {
  let t = {};
  if (e) {
    let r = e.indexOf("#");
    r >= 0 && (t.hash = e.substring(r), e = e.substring(0, r));
    let n = e.indexOf("?");
    n >= 0 && (t.search = e.substring(n), e = e.substring(0, n)), e && (t.pathname = e);
  }
  return t;
}
function ca(e, t, r, n = {}) {
  let { window: a = document.defaultView, v5Compat: o = !1 } = n, l = a.history, s = "POP", u = null, c = h();
  c == null && (c = 0, l.replaceState({ ...l.state, idx: c }, ""));
  function h() {
    return (l.state || { idx: null }).idx;
  }
  function v() {
    s = "POP";
    let b = h(), P = b == null ? null : b - c;
    c = b, u && u({ action: s, location: L.location, delta: P });
  }
  function g(b, P) {
    s = "PUSH";
    let C = Ke(L.location, b, P);
    c = h() + 1;
    let T = qt(C, c), O = L.createHref(C);
    try {
      l.pushState(T, "", O);
    } catch (f) {
      if (f instanceof DOMException && f.name === "DataCloneError")
        throw f;
      a.location.assign(O);
    }
    o && u && u({ action: s, location: L.location, delta: 1 });
  }
  function y(b, P) {
    s = "REPLACE";
    let C = Ke(L.location, b, P);
    c = h();
    let T = qt(C, c), O = L.createHref(C);
    l.replaceState(T, "", O), o && u && u({ action: s, location: L.location, delta: 0 });
  }
  function E(b) {
    let P = a.location.origin !== "null" ? a.location.origin : a.location.href, C = typeof b == "string" ? b : ve(b);
    return C = C.replace(/ $/, "%20"), z(
      P,
      `No window.location.(origin|href) available to create URL for href: ${C}`
    ), new URL(C, P);
  }
  let L = {
    get action() {
      return s;
    },
    get location() {
      return e(a, l);
    },
    listen(b) {
      if (u)
        throw new Error("A history only accepts one active listener");
      return a.addEventListener(Xt, v), u = b, () => {
        a.removeEventListener(Xt, v), u = null;
      };
    },
    createHref(b) {
      return t(a, b);
    },
    createURL: E,
    encodeLocation(b) {
      let P = E(b);
      return {
        pathname: P.pathname,
        search: P.search,
        hash: P.hash
      };
    },
    push: g,
    replace: y,
    go(b) {
      return l.go(b);
    }
  };
  return L;
}
var We, Qt = class {
  constructor(e) {
    if (la(this, We, /* @__PURE__ */ new Map()), e)
      for (let [t, r] of e)
        this.set(t, r);
  }
  get(e) {
    if (bt(this, We).has(e))
      return bt(this, We).get(e);
    if (e.defaultValue !== void 0)
      return e.defaultValue;
    throw new Error("No value found for context");
  }
  set(e, t) {
    bt(this, We).set(e, t);
  }
};
We = /* @__PURE__ */ new WeakMap();
var da = /* @__PURE__ */ new Set([
  "lazy",
  "caseSensitive",
  "path",
  "id",
  "index",
  "children"
]);
function fa(e) {
  return da.has(
    e
  );
}
var ha = /* @__PURE__ */ new Set([
  "lazy",
  "caseSensitive",
  "path",
  "id",
  "index",
  "unstable_middleware",
  "children"
]);
function ma(e) {
  return ha.has(
    e
  );
}
function pa(e) {
  return e.index === !0;
}
function dt(e, t, r = [], n = {}) {
  return e.map((a, o) => {
    let l = [...r, String(o)], s = typeof a.id == "string" ? a.id : l.join("-");
    if (z(
      a.index !== !0 || !a.children,
      "Cannot specify children on an index route"
    ), z(
      !n[s],
      `Found a route id collision on id "${s}".  Route id's must be globally unique within Data Router usages`
    ), pa(a)) {
      let u = {
        ...a,
        ...t(a),
        id: s
      };
      return n[s] = u, u;
    } else {
      let u = {
        ...a,
        ...t(a),
        id: s,
        children: void 0
      };
      return n[s] = u, a.children && (u.children = dt(
        a.children,
        t,
        l,
        n
      )), u;
    }
  });
}
function pe(e, t, r = "/") {
  return st(e, t, r, !1);
}
function st(e, t, r, n) {
  let a = typeof t == "string" ? ye(t) : t, o = re(a.pathname || "/", r);
  if (o == null)
    return null;
  let l = Rr(e);
  ya(l);
  let s = null;
  for (let u = 0; s == null && u < l.length; ++u) {
    let c = Da(o);
    s = Ca(
      l[u],
      c,
      n
    );
  }
  return s;
}
function va(e, t) {
  let { route: r, pathname: n, params: a } = e;
  return {
    id: r.id,
    pathname: n,
    params: a,
    data: t[r.id],
    handle: r.handle
  };
}
function Rr(e, t = [], r = [], n = "") {
  let a = (o, l, s) => {
    let u = {
      relativePath: s === void 0 ? o.path || "" : s,
      caseSensitive: o.caseSensitive === !0,
      childrenIndex: l,
      route: o
    };
    u.relativePath.startsWith("/") && (z(
      u.relativePath.startsWith(n),
      `Absolute route path "${u.relativePath}" nested under path "${n}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`
    ), u.relativePath = u.relativePath.slice(n.length));
    let c = ie([n, u.relativePath]), h = r.concat(u);
    o.children && o.children.length > 0 && (z(
      // Our types know better, but runtime JS may not!
      // @ts-expect-error
      o.index !== !0,
      `Index routes must not have child routes. Please remove all child routes from route path "${c}".`
    ), Rr(o.children, t, h, c)), !(o.path == null && !o.index) && t.push({
      path: c,
      score: Pa(c, o.index),
      routesMeta: h
    });
  };
  return e.forEach((o, l) => {
    var s;
    if (o.path === "" || !((s = o.path) != null && s.includes("?")))
      a(o, l);
    else
      for (let u of Er(o.path))
        a(o, l, u);
  }), t;
}
function Er(e) {
  let t = e.split("/");
  if (t.length === 0) return [];
  let [r, ...n] = t, a = r.endsWith("?"), o = r.replace(/\?$/, "");
  if (n.length === 0)
    return a ? [o, ""] : [o];
  let l = Er(n.join("/")), s = [];
  return s.push(
    ...l.map(
      (u) => u === "" ? o : [o, u].join("/")
    )
  ), a && s.push(...l), s.map(
    (u) => e.startsWith("/") && u === "" ? "/" : u
  );
}
function ya(e) {
  e.sort(
    (t, r) => t.score !== r.score ? r.score - t.score : La(
      t.routesMeta.map((n) => n.childrenIndex),
      r.routesMeta.map((n) => n.childrenIndex)
    )
  );
}
var ga = /^:[\w-]+$/, wa = 3, ba = 2, Ra = 1, Ea = 10, xa = -2, Zt = (e) => e === "*";
function Pa(e, t) {
  let r = e.split("/"), n = r.length;
  return r.some(Zt) && (n += xa), t && (n += ba), r.filter((a) => !Zt(a)).reduce(
    (a, o) => a + (ga.test(o) ? wa : o === "" ? Ra : Ea),
    n
  );
}
function La(e, t) {
  return e.length === t.length && e.slice(0, -1).every((n, a) => n === t[a]) ? (
    // If two routes are siblings, we should try to match the earlier sibling
    // first. This allows people to have fine-grained control over the matching
    // behavior by simply putting routes with identical paths in the order they
    // want them tried.
    e[e.length - 1] - t[t.length - 1]
  ) : (
    // Otherwise, it doesn't really make sense to rank non-siblings by index,
    // so they sort equally.
    0
  );
}
function Ca(e, t, r = !1) {
  let { routesMeta: n } = e, a = {}, o = "/", l = [];
  for (let s = 0; s < n.length; ++s) {
    let u = n[s], c = s === n.length - 1, h = o === "/" ? t : t.slice(o.length) || "/", v = ft(
      { path: u.relativePath, caseSensitive: u.caseSensitive, end: c },
      h
    ), g = u.route;
    if (!v && c && r && !n[n.length - 1].route.index && (v = ft(
      {
        path: u.relativePath,
        caseSensitive: u.caseSensitive,
        end: !1
      },
      h
    )), !v)
      return null;
    Object.assign(a, v.params), l.push({
      // TODO: Can this as be avoided?
      params: a,
      pathname: ie([o, v.pathname]),
      pathnameBase: Fa(
        ie([o, v.pathnameBase])
      ),
      route: g
    }), v.pathnameBase !== "/" && (o = ie([o, v.pathnameBase]));
  }
  return l;
}
function ft(e, t) {
  typeof e == "string" && (e = { path: e, caseSensitive: !1, end: !0 });
  let [r, n] = Sa(
    e.path,
    e.caseSensitive,
    e.end
  ), a = t.match(r);
  if (!a) return null;
  let o = a[0], l = o.replace(/(.)\/+$/, "$1"), s = a.slice(1);
  return {
    params: n.reduce(
      (c, { paramName: h, isOptional: v }, g) => {
        if (h === "*") {
          let E = s[g] || "";
          l = o.slice(0, o.length - E.length).replace(/(.)\/+$/, "$1");
        }
        const y = s[g];
        return v && !y ? c[h] = void 0 : c[h] = (y || "").replace(/%2F/g, "/"), c;
      },
      {}
    ),
    pathname: o,
    pathnameBase: l,
    pattern: e
  };
}
function Sa(e, t = !1, r = !0) {
  K(
    e === "*" || !e.endsWith("*") || e.endsWith("/*"),
    `Route path "${e}" will be treated as if it were "${e.replace(/\*$/, "/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${e.replace(/\*$/, "/*")}".`
  );
  let n = [], a = "^" + e.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(
    /\/:([\w-]+)(\?)?/g,
    (l, s, u) => (n.push({ paramName: s, isOptional: u != null }), u ? "/?([^\\/]+)?" : "/([^\\/]+)")
  );
  return e.endsWith("*") ? (n.push({ paramName: "*" }), a += e === "*" || e === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$") : r ? a += "\\/*$" : e !== "" && e !== "/" && (a += "(?:(?=\\/|$))"), [new RegExp(a, t ? void 0 : "i"), n];
}
function Da(e) {
  try {
    return e.split("/").map((t) => decodeURIComponent(t).replace(/\//g, "%2F")).join("/");
  } catch (t) {
    return K(
      !1,
      `The URL path "${e}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${t}).`
    ), e;
  }
}
function re(e, t) {
  if (t === "/") return e;
  if (!e.toLowerCase().startsWith(t.toLowerCase()))
    return null;
  let r = t.endsWith("/") ? t.length - 1 : t.length, n = e.charAt(r);
  return n && n !== "/" ? null : e.slice(r) || "/";
}
function Ma(e, t = "/") {
  let {
    pathname: r,
    search: n = "",
    hash: a = ""
  } = typeof e == "string" ? ye(e) : e;
  return {
    pathname: r ? r.startsWith("/") ? r : Ta(r, t) : t,
    search: $a(n),
    hash: ka(a)
  };
}
function Ta(e, t) {
  let r = t.replace(/\/+$/, "").split("/");
  return e.split("/").forEach((a) => {
    a === ".." ? r.length > 1 && r.pop() : a !== "." && r.push(a);
  }), r.length > 1 ? r.join("/") : "/";
}
function Rt(e, t, r, n) {
  return `Cannot include a '${e}' character in a manually specified \`to.${t}\` field [${JSON.stringify(
    n
  )}].  Please separate it out to the \`to.${r}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`;
}
function xr(e) {
  return e.filter(
    (t, r) => r === 0 || t.route.path && t.route.path.length > 0
  );
}
function Dt(e) {
  let t = xr(e);
  return t.map(
    (r, n) => n === t.length - 1 ? r.pathname : r.pathnameBase
  );
}
function Mt(e, t, r, n = !1) {
  let a;
  typeof e == "string" ? a = ye(e) : (a = { ...e }, z(
    !a.pathname || !a.pathname.includes("?"),
    Rt("?", "pathname", "search", a)
  ), z(
    !a.pathname || !a.pathname.includes("#"),
    Rt("#", "pathname", "hash", a)
  ), z(
    !a.search || !a.search.includes("#"),
    Rt("#", "search", "hash", a)
  ));
  let o = e === "" || a.pathname === "", l = o ? "/" : a.pathname, s;
  if (l == null)
    s = r;
  else {
    let v = t.length - 1;
    if (!n && l.startsWith("..")) {
      let g = l.split("/");
      for (; g[0] === ".."; )
        g.shift(), v -= 1;
      a.pathname = g.join("/");
    }
    s = v >= 0 ? t[v] : "/";
  }
  let u = Ma(a, s), c = l && l !== "/" && l.endsWith("/"), h = (o || l === ".") && r.endsWith("/");
  return !u.pathname.endsWith("/") && (c || h) && (u.pathname += "/"), u;
}
var ie = (e) => e.join("/").replace(/\/\/+/g, "/"), Fa = (e) => e.replace(/\/+$/, "").replace(/^\/*/, "/"), $a = (e) => !e || e === "?" ? "" : e.startsWith("?") ? e : "?" + e, ka = (e) => !e || e === "#" ? "" : e.startsWith("#") ? e : "#" + e, ht = class {
  constructor(e, t, r, n = !1) {
    this.status = e, this.statusText = t || "", this.internal = n, r instanceof Error ? (this.data = r.toString(), this.error = r) : this.data = r;
  }
};
function Ye(e) {
  return e != null && typeof e.status == "number" && typeof e.statusText == "string" && typeof e.internal == "boolean" && "data" in e;
}
var Pr = [
  "POST",
  "PUT",
  "PATCH",
  "DELETE"
], Ua = new Set(
  Pr
), Na = [
  "GET",
  ...Pr
], za = new Set(Na), Oa = /* @__PURE__ */ new Set([301, 302, 303, 307, 308]), Aa = /* @__PURE__ */ new Set([307, 308]), Et = {
  state: "idle",
  location: void 0,
  formMethod: void 0,
  formAction: void 0,
  formEncType: void 0,
  formData: void 0,
  json: void 0,
  text: void 0
}, _a = {
  state: "idle",
  data: void 0,
  formMethod: void 0,
  formAction: void 0,
  formEncType: void 0,
  formData: void 0,
  json: void 0,
  text: void 0
}, je = {
  state: "unblocked",
  proceed: void 0,
  reset: void 0,
  location: void 0
}, Tt = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i, Ia = (e) => ({
  hasErrorBoundary: !!e.hasErrorBoundary
}), Lr = "remix-router-transitions", Cr = Symbol("ResetLoaderData");
function ja(e) {
  const t = e.window ? e.window : typeof window < "u" ? window : void 0, r = typeof t < "u" && typeof t.document < "u" && typeof t.document.createElement < "u";
  z(
    e.routes.length > 0,
    "You must provide a non-empty routes array to createRouter"
  );
  let n = e.mapRouteProperties || Ia, a = {}, o = dt(
    e.routes,
    n,
    void 0,
    a
  ), l, s = e.basename || "/", u = e.dataStrategy || Ya, c = {
    unstable_middleware: !1,
    ...e.future
  }, h = null, v = /* @__PURE__ */ new Set(), g = null, y = null, E = null, L = e.hydrationData != null, b = pe(o, e.history.location, s), P = !1, C = null;
  if (b == null && !e.patchRoutesOnNavigation) {
    let i = te(404, {
      pathname: e.history.location.pathname
    }), { matches: d, route: m } = hr(o);
    b = d, C = { [m.id]: i };
  }
  b && !e.hydrationData && tt(
    b,
    o,
    e.history.location.pathname
  ).active && (b = null);
  let T;
  if (b)
    if (b.some((i) => i.route.lazy))
      T = !1;
    else if (!b.some((i) => i.route.loader))
      T = !0;
    else {
      let i = e.hydrationData ? e.hydrationData.loaderData : null, d = e.hydrationData ? e.hydrationData.errors : null;
      if (d) {
        let m = b.findIndex(
          (w) => d[w.route.id] !== void 0
        );
        T = b.slice(0, m + 1).every((w) => !Ct(w.route, i, d));
      } else
        T = b.every(
          (m) => !Ct(m.route, i, d)
        );
    }
  else {
    T = !1, b = [];
    let i = tt(
      null,
      o,
      e.history.location.pathname
    );
    i.active && i.matches && (P = !0, b = i.matches);
  }
  let O, f = {
    historyAction: e.history.action,
    location: e.history.location,
    matches: b,
    initialized: T,
    navigation: Et,
    // Don't restore on initial updateState() if we were SSR'd
    restoreScrollPosition: e.hydrationData != null ? !1 : null,
    preventScrollReset: !1,
    revalidation: "idle",
    loaderData: e.hydrationData && e.hydrationData.loaderData || {},
    actionData: e.hydrationData && e.hydrationData.actionData || null,
    errors: e.hydrationData && e.hydrationData.errors || C,
    fetchers: /* @__PURE__ */ new Map(),
    blockers: /* @__PURE__ */ new Map()
  }, U = "POP", j = !1, N, _ = !1, Y = /* @__PURE__ */ new Map(), ee = null, De = !1, ge = !1, qe = /* @__PURE__ */ new Set(), G = /* @__PURE__ */ new Map(), Qe = 0, Ne = -1, Me = /* @__PURE__ */ new Map(), ue = /* @__PURE__ */ new Set(), Te = /* @__PURE__ */ new Map(), ze = /* @__PURE__ */ new Map(), ne = /* @__PURE__ */ new Set(), we = /* @__PURE__ */ new Map(), Ze, be = null;
  function Br() {
    if (h = e.history.listen(
      ({ action: i, location: d, delta: m }) => {
        if (Ze) {
          Ze(), Ze = void 0;
          return;
        }
        K(
          we.size === 0 || m != null,
          "You are trying to use a blocker on a POP navigation to a location that was not created by @remix-run/router. This will fail silently in production. This can happen if you are navigating outside the router via `window.history.pushState`/`window.location.hash` instead of using router navigation APIs.  This can also happen if you are using createHashRouter and the user manually changes the URL."
        );
        let w = Kt({
          currentLocation: f.location,
          nextLocation: d,
          historyAction: i
        });
        if (w && m != null) {
          let R = new Promise(($) => {
            Ze = $;
          });
          e.history.go(m * -1), et(w, {
            state: "blocked",
            location: d,
            proceed() {
              et(w, {
                state: "proceeding",
                proceed: void 0,
                reset: void 0,
                location: d
              }), R.then(() => e.history.go(m));
            },
            reset() {
              let $ = new Map(f.blockers);
              $.set(w, je), Q({ blockers: $ });
            }
          });
          return;
        }
        return Re(i, d);
      }
    ), r) {
      on(t, Y);
      let i = () => ln(t, Y);
      t.addEventListener("pagehide", i), ee = () => t.removeEventListener("pagehide", i);
    }
    return f.initialized || Re("POP", f.location, {
      initialHydration: !0
    }), O;
  }
  function Wr() {
    h && h(), ee && ee(), v.clear(), N && N.abort(), f.fetchers.forEach((i, d) => yt(d)), f.blockers.forEach((i, d) => Vt(d));
  }
  function Vr(i) {
    return v.add(i), () => v.delete(i);
  }
  function Q(i, d = {}) {
    f = {
      ...f,
      ...i
    };
    let m = [], w = [];
    f.fetchers.forEach((R, $) => {
      R.state === "idle" && (ne.has($) ? m.push($) : w.push($));
    }), ne.forEach((R) => {
      !f.fetchers.has(R) && !G.has(R) && m.push(R);
    }), [...v].forEach(
      (R) => R(f, {
        deletedFetchers: m,
        viewTransitionOpts: d.viewTransitionOpts,
        flushSync: d.flushSync === !0
      })
    ), m.forEach((R) => yt(R)), w.forEach((R) => f.fetchers.delete(R));
  }
  function Fe(i, d, { flushSync: m } = {}) {
    var x, D;
    let w = f.actionData != null && f.navigation.formMethod != null && ae(f.navigation.formMethod) && f.navigation.state === "loading" && ((x = i.state) == null ? void 0 : x._isRedirect) !== !0, R;
    d.actionData ? Object.keys(d.actionData).length > 0 ? R = d.actionData : R = null : w ? R = f.actionData : R = null;
    let $ = d.loaderData ? dr(
      f.loaderData,
      d.loaderData,
      d.matches || [],
      d.errors
    ) : f.loaderData, F = f.blockers;
    F.size > 0 && (F = new Map(F), F.forEach((k, A) => F.set(A, je)));
    let S = j === !0 || f.navigation.formMethod != null && ae(f.navigation.formMethod) && ((D = i.state) == null ? void 0 : D._isRedirect) !== !0;
    l && (o = l, l = void 0), De || U === "POP" || (U === "PUSH" ? e.history.push(i, i.state) : U === "REPLACE" && e.history.replace(i, i.state));
    let M;
    if (U === "POP") {
      let k = Y.get(f.location.pathname);
      k && k.has(i.pathname) ? M = {
        currentLocation: f.location,
        nextLocation: i
      } : Y.has(i.pathname) && (M = {
        currentLocation: i,
        nextLocation: f.location
      });
    } else if (_) {
      let k = Y.get(f.location.pathname);
      k ? k.add(i.pathname) : (k = /* @__PURE__ */ new Set([i.pathname]), Y.set(f.location.pathname, k)), M = {
        currentLocation: f.location,
        nextLocation: i
      };
    }
    Q(
      {
        ...d,
        // matches, errors, fetchers go through as-is
        actionData: R,
        loaderData: $,
        historyAction: U,
        location: i,
        initialized: !0,
        navigation: Et,
        revalidation: "idle",
        restoreScrollPosition: Jt(
          i,
          d.matches || f.matches
        ),
        preventScrollReset: S,
        blockers: F
      },
      {
        viewTransitionOpts: M,
        flushSync: m === !0
      }
    ), U = "POP", j = !1, _ = !1, De = !1, ge = !1, be == null || be.resolve(), be = null;
  }
  async function At(i, d) {
    if (typeof i == "number") {
      e.history.go(i);
      return;
    }
    let m = Lt(
      f.location,
      f.matches,
      s,
      i,
      d == null ? void 0 : d.fromRouteId,
      d == null ? void 0 : d.relative
    ), { path: w, submission: R, error: $ } = er(
      !1,
      m,
      d
    ), F = f.location, S = Ke(f.location, w, d && d.state);
    S = {
      ...S,
      ...e.history.encodeLocation(S)
    };
    let M = d && d.replace != null ? d.replace : void 0, x = "PUSH";
    M === !0 ? x = "REPLACE" : M === !1 || R != null && ae(R.formMethod) && R.formAction === f.location.pathname + f.location.search && (x = "REPLACE");
    let D = d && "preventScrollReset" in d ? d.preventScrollReset === !0 : void 0, k = (d && d.flushSync) === !0, A = Kt({
      currentLocation: F,
      nextLocation: S,
      historyAction: x
    });
    if (A) {
      et(A, {
        state: "blocked",
        location: S,
        proceed() {
          et(A, {
            state: "proceeding",
            proceed: void 0,
            reset: void 0,
            location: S
          }), At(i, d);
        },
        reset() {
          let H = new Map(f.blockers);
          H.set(A, je), Q({ blockers: H });
        }
      });
      return;
    }
    await Re(x, S, {
      submission: R,
      // Send through the formData serialization error if we have one so we can
      // render at the right error boundary after we match routes
      pendingError: $,
      preventScrollReset: D,
      replace: d && d.replace,
      enableViewTransition: d && d.viewTransition,
      flushSync: k
    });
  }
  function Kr() {
    be || (be = sn()), vt(), Q({ revalidation: "loading" });
    let i = be.promise;
    return f.navigation.state === "submitting" ? i : f.navigation.state === "idle" ? (Re(f.historyAction, f.location, {
      startUninterruptedRevalidation: !0
    }), i) : (Re(
      U || f.historyAction,
      f.navigation.location,
      {
        overrideNavigation: f.navigation,
        // Proxy through any rending view transition
        enableViewTransition: _ === !0
      }
    ), i);
  }
  async function Re(i, d, m) {
    N && N.abort(), N = null, U = i, De = (m && m.startUninterruptedRevalidation) === !0, ra(f.location, f.matches), j = (m && m.preventScrollReset) === !0, _ = (m && m.enableViewTransition) === !0;
    let w = l || o, R = m && m.overrideNavigation, $ = m != null && m.initialHydration && f.matches && f.matches.length > 0 && !P ? (
      // `matchRoutes()` has already been called if we're in here via `router.initialize()`
      f.matches
    ) : pe(w, d, s), F = (m && m.flushSync) === !0;
    if ($ && f.initialized && !ge && en(f.location, d) && !(m && m.submission && ae(m.submission.formMethod))) {
      Fe(d, { matches: $ }, { flushSync: F });
      return;
    }
    let S = tt($, w, d.pathname);
    if (S.active && S.matches && ($ = S.matches), !$) {
      let { error: W, notFoundMatches: B, route: J } = gt(
        d.pathname
      );
      Fe(
        d,
        {
          matches: B,
          loaderData: {},
          errors: {
            [J.id]: W
          }
        },
        { flushSync: F }
      );
      return;
    }
    N = new AbortController();
    let M = Ue(
      e.history,
      d,
      N.signal,
      m && m.submission
    ), x = new Qt(
      e.unstable_getContext ? await e.unstable_getContext() : void 0
    ), D;
    if (m && m.pendingError)
      D = [
        Pe($).route.id,
        { type: "error", error: m.pendingError }
      ];
    else if (m && m.submission && ae(m.submission.formMethod)) {
      let W = await Yr(
        M,
        d,
        m.submission,
        $,
        x,
        S.active,
        { replace: m.replace, flushSync: F }
      );
      if (W.shortCircuited)
        return;
      if (W.pendingActionResult) {
        let [B, J] = W.pendingActionResult;
        if (Z(J) && Ye(J.error) && J.error.status === 404) {
          N = null, Fe(d, {
            matches: W.matches,
            loaderData: {},
            errors: {
              [B]: J.error
            }
          });
          return;
        }
      }
      $ = W.matches || $, D = W.pendingActionResult, R = xt(d, m.submission), F = !1, S.active = !1, M = Ue(
        e.history,
        M.url,
        M.signal
      );
    }
    let {
      shortCircuited: k,
      matches: A,
      loaderData: H,
      errors: X
    } = await Jr(
      M,
      d,
      $,
      x,
      S.active,
      R,
      m && m.submission,
      m && m.fetcherSubmission,
      m && m.replace,
      m && m.initialHydration === !0,
      F,
      D
    );
    k || (N = null, Fe(d, {
      matches: A || $,
      ...fr(D),
      loaderData: H,
      errors: X
    }));
  }
  async function Yr(i, d, m, w, R, $, F = {}) {
    vt();
    let S = an(d, m);
    if (Q({ navigation: S }, { flushSync: F.flushSync === !0 }), $) {
      let D = await rt(
        w,
        d.pathname,
        i.signal
      );
      if (D.type === "aborted")
        return { shortCircuited: !0 };
      if (D.type === "error") {
        let k = Pe(D.partialMatches).route.id;
        return {
          matches: D.partialMatches,
          pendingActionResult: [
            k,
            {
              type: "error",
              error: D.error
            }
          ]
        };
      } else if (D.matches)
        w = D.matches;
      else {
        let { notFoundMatches: k, error: A, route: H } = gt(
          d.pathname
        );
        return {
          matches: k,
          pendingActionResult: [
            H.id,
            {
              type: "error",
              error: A
            }
          ]
        };
      }
    }
    let M, x = Ve(w, d);
    if (!x.route.action && !x.route.lazy)
      M = {
        type: "error",
        error: te(405, {
          method: i.method,
          pathname: d.pathname,
          routeId: x.route.id
        })
      };
    else {
      let D = await Oe(
        "action",
        i,
        [x],
        w,
        R,
        null
      );
      if (M = D[x.route.id], !M) {
        for (let k of w)
          if (D[k.route.id]) {
            M = D[k.route.id];
            break;
          }
      }
      if (i.signal.aborted)
        return { shortCircuited: !0 };
    }
    if (Le(M)) {
      let D;
      return F && F.replace != null ? D = F.replace : D = sr(
        M.response.headers.get("Location"),
        new URL(i.url),
        s
      ) === f.location.pathname + f.location.search, await Ee(i, M, !0, {
        submission: m,
        replace: D
      }), { shortCircuited: !0 };
    }
    if (Z(M)) {
      let D = Pe(w, x.route.id);
      return (F && F.replace) !== !0 && (U = "PUSH"), {
        matches: w,
        pendingActionResult: [D.route.id, M]
      };
    }
    return {
      matches: w,
      pendingActionResult: [x.route.id, M]
    };
  }
  async function Jr(i, d, m, w, R, $, F, S, M, x, D, k) {
    let A = $ || xt(d, F), H = F || S || pr(A), X = !De && !x;
    if (R) {
      if (X) {
        let q = _t(k);
        Q(
          {
            navigation: A,
            ...q !== void 0 ? { actionData: q } : {}
          },
          {
            flushSync: D
          }
        );
      }
      let I = await rt(
        m,
        d.pathname,
        i.signal
      );
      if (I.type === "aborted")
        return { shortCircuited: !0 };
      if (I.type === "error") {
        let q = Pe(I.partialMatches).route.id;
        return {
          matches: I.partialMatches,
          loaderData: {},
          errors: {
            [q]: I.error
          }
        };
      } else if (I.matches)
        m = I.matches;
      else {
        let { error: q, notFoundMatches: ot, route: Ie } = gt(
          d.pathname
        );
        return {
          matches: ot,
          loaderData: {},
          errors: {
            [Ie.id]: q
          }
        };
      }
    }
    let W = l || o, [B, J] = rr(
      e.history,
      f,
      m,
      H,
      d,
      x === !0,
      ge,
      qe,
      ne,
      Te,
      ue,
      W,
      s,
      k
    );
    if (Ne = ++Qe, B.length === 0 && J.length === 0) {
      let I = Bt();
      return Fe(
        d,
        {
          matches: m,
          loaderData: {},
          // Commit pending error if we're short circuiting
          errors: k && Z(k[1]) ? { [k[0]]: k[1].error } : null,
          ...fr(k),
          ...I ? { fetchers: new Map(f.fetchers) } : {}
        },
        { flushSync: D }
      ), { shortCircuited: !0 };
    }
    if (X) {
      let I = {};
      if (!R) {
        I.navigation = A;
        let q = _t(k);
        q !== void 0 && (I.actionData = q);
      }
      J.length > 0 && (I.fetchers = Gr(J)), Q(I, { flushSync: D });
    }
    J.forEach((I) => {
      he(I.key), I.controller && G.set(I.key, I.controller);
    });
    let $e = () => J.forEach((I) => he(I.key));
    N && N.signal.addEventListener(
      "abort",
      $e
    );
    let { loaderResults: Ae, fetcherResults: de } = await It(
      m,
      B,
      J,
      i,
      w
    );
    if (i.signal.aborted)
      return { shortCircuited: !0 };
    N && N.signal.removeEventListener(
      "abort",
      $e
    ), J.forEach((I) => G.delete(I.key));
    let oe = it(Ae);
    if (oe)
      return await Ee(i, oe.result, !0, {
        replace: M
      }), { shortCircuited: !0 };
    if (oe = it(de), oe)
      return ue.add(oe.key), await Ee(i, oe.result, !0, {
        replace: M
      }), { shortCircuited: !0 };
    let { loaderData: wt, errors: _e } = cr(
      f,
      m,
      Ae,
      k,
      J,
      de
    );
    x && f.errors && (_e = { ...f.errors, ..._e });
    let xe = Bt(), at = Wt(Ne), nt = xe || at || J.length > 0;
    return {
      matches: m,
      loaderData: wt,
      errors: _e,
      ...nt ? { fetchers: new Map(f.fetchers) } : {}
    };
  }
  function _t(i) {
    if (i && !Z(i[1]))
      return {
        [i[0]]: i[1].data
      };
    if (f.actionData)
      return Object.keys(f.actionData).length === 0 ? null : f.actionData;
  }
  function Gr(i) {
    return i.forEach((d) => {
      let m = f.fetchers.get(d.key), w = He(
        void 0,
        m ? m.data : void 0
      );
      f.fetchers.set(d.key, w);
    }), new Map(f.fetchers);
  }
  async function Xr(i, d, m, w) {
    he(i);
    let R = (w && w.flushSync) === !0, $ = l || o, F = Lt(
      f.location,
      f.matches,
      s,
      m,
      d,
      w == null ? void 0 : w.relative
    ), S = pe($, F, s), M = tt(S, $, F);
    if (M.active && M.matches && (S = M.matches), !S) {
      ce(
        i,
        d,
        te(404, { pathname: F }),
        { flushSync: R }
      );
      return;
    }
    let { path: x, submission: D, error: k } = er(
      !0,
      F,
      w
    );
    if (k) {
      ce(i, d, k, { flushSync: R });
      return;
    }
    let A = Ve(S, x), H = new Qt(
      e.unstable_getContext ? await e.unstable_getContext() : void 0
    ), X = (w && w.preventScrollReset) === !0;
    if (D && ae(D.formMethod)) {
      await qr(
        i,
        d,
        x,
        A,
        S,
        H,
        M.active,
        R,
        X,
        D
      );
      return;
    }
    Te.set(i, { routeId: d, path: x }), await Qr(
      i,
      d,
      x,
      A,
      S,
      H,
      M.active,
      R,
      X,
      D
    );
  }
  async function qr(i, d, m, w, R, $, F, S, M, x) {
    vt(), Te.delete(i);
    function D(V) {
      if (!V.route.action && !V.route.lazy) {
        let ke = te(405, {
          method: x.formMethod,
          pathname: m,
          routeId: d
        });
        return ce(i, d, ke, { flushSync: S }), !0;
      }
      return !1;
    }
    if (!F && D(w))
      return;
    let k = f.fetchers.get(i);
    fe(i, nn(x, k), {
      flushSync: S
    });
    let A = new AbortController(), H = Ue(
      e.history,
      m,
      A.signal,
      x
    );
    if (F) {
      let V = await rt(
        R,
        m,
        H.signal,
        i
      );
      if (V.type === "aborted")
        return;
      if (V.type === "error") {
        ce(i, d, V.error, { flushSync: S });
        return;
      } else if (V.matches) {
        if (R = V.matches, w = Ve(R, m), D(w))
          return;
      } else {
        ce(
          i,
          d,
          te(404, { pathname: m }),
          { flushSync: S }
        );
        return;
      }
    }
    G.set(i, A);
    let X = Qe, B = (await Oe(
      "action",
      H,
      [w],
      R,
      $,
      i
    ))[w.route.id];
    if (H.signal.aborted) {
      G.get(i) === A && G.delete(i);
      return;
    }
    if (ne.has(i)) {
      if (Le(B) || Z(B)) {
        fe(i, me(void 0));
        return;
      }
    } else {
      if (Le(B))
        if (G.delete(i), Ne > X) {
          fe(i, me(void 0));
          return;
        } else
          return ue.add(i), fe(i, He(x)), Ee(H, B, !1, {
            fetcherSubmission: x,
            preventScrollReset: M
          });
      if (Z(B)) {
        ce(i, d, B.error);
        return;
      }
    }
    let J = f.navigation.location || f.location, $e = Ue(
      e.history,
      J,
      A.signal
    ), Ae = l || o, de = f.navigation.state !== "idle" ? pe(Ae, f.navigation.location, s) : f.matches;
    z(de, "Didn't find any matches after fetcher action");
    let oe = ++Qe;
    Me.set(i, oe);
    let wt = He(x, B.data);
    f.fetchers.set(i, wt);
    let [_e, xe] = rr(
      e.history,
      f,
      de,
      x,
      J,
      !1,
      ge,
      qe,
      ne,
      Te,
      ue,
      Ae,
      s,
      [w.route.id, B]
    );
    xe.filter((V) => V.key !== i).forEach((V) => {
      let ke = V.key, Gt = f.fetchers.get(ke), oa = He(
        void 0,
        Gt ? Gt.data : void 0
      );
      f.fetchers.set(ke, oa), he(ke), V.controller && G.set(ke, V.controller);
    }), Q({ fetchers: new Map(f.fetchers) });
    let at = () => xe.forEach((V) => he(V.key));
    A.signal.addEventListener(
      "abort",
      at
    );
    let { loaderResults: nt, fetcherResults: I } = await It(
      de,
      _e,
      xe,
      $e,
      $
    );
    if (A.signal.aborted)
      return;
    A.signal.removeEventListener(
      "abort",
      at
    ), Me.delete(i), G.delete(i), xe.forEach((V) => G.delete(V.key));
    let q = it(nt);
    if (q)
      return Ee(
        $e,
        q.result,
        !1,
        { preventScrollReset: M }
      );
    if (q = it(I), q)
      return ue.add(q.key), Ee(
        $e,
        q.result,
        !1,
        { preventScrollReset: M }
      );
    let { loaderData: ot, errors: Ie } = cr(
      f,
      de,
      nt,
      void 0,
      xe,
      I
    );
    if (f.fetchers.has(i)) {
      let V = me(B.data);
      f.fetchers.set(i, V);
    }
    Wt(oe), f.navigation.state === "loading" && oe > Ne ? (z(U, "Expected pending action"), N && N.abort(), Fe(f.navigation.location, {
      matches: de,
      loaderData: ot,
      errors: Ie,
      fetchers: new Map(f.fetchers)
    })) : (Q({
      errors: Ie,
      loaderData: dr(
        f.loaderData,
        ot,
        de,
        Ie
      ),
      fetchers: new Map(f.fetchers)
    }), ge = !1);
  }
  async function Qr(i, d, m, w, R, $, F, S, M, x) {
    let D = f.fetchers.get(i);
    fe(
      i,
      He(
        x,
        D ? D.data : void 0
      ),
      { flushSync: S }
    );
    let k = new AbortController(), A = Ue(
      e.history,
      m,
      k.signal
    );
    if (F) {
      let B = await rt(
        R,
        m,
        A.signal,
        i
      );
      if (B.type === "aborted")
        return;
      if (B.type === "error") {
        ce(i, d, B.error, { flushSync: S });
        return;
      } else if (B.matches)
        R = B.matches, w = Ve(R, m);
      else {
        ce(
          i,
          d,
          te(404, { pathname: m }),
          { flushSync: S }
        );
        return;
      }
    }
    G.set(i, k);
    let H = Qe, W = (await Oe(
      "loader",
      A,
      [w],
      R,
      $,
      i
    ))[w.route.id];
    if (G.get(i) === k && G.delete(i), !A.signal.aborted) {
      if (ne.has(i)) {
        fe(i, me(void 0));
        return;
      }
      if (Le(W))
        if (Ne > H) {
          fe(i, me(void 0));
          return;
        } else {
          ue.add(i), await Ee(A, W, !1, {
            preventScrollReset: M
          });
          return;
        }
      if (Z(W)) {
        ce(i, d, W.error);
        return;
      }
      fe(i, me(W.data));
    }
  }
  async function Ee(i, d, m, {
    submission: w,
    fetcherSubmission: R,
    preventScrollReset: $,
    replace: F
  } = {}) {
    d.response.headers.has("X-Remix-Revalidate") && (ge = !0);
    let S = d.response.headers.get("Location");
    z(S, "Expected a Location header on the redirect Response"), S = sr(
      S,
      new URL(i.url),
      s
    );
    let M = Ke(f.location, S, {
      _isRedirect: !0
    });
    if (r) {
      let X = !1;
      if (d.response.headers.has("X-Remix-Reload-Document"))
        X = !0;
      else if (Tt.test(S)) {
        const W = e.history.createURL(S);
        X = // Hard reload if it's an absolute URL to a new origin
        W.origin !== t.location.origin || // Hard reload if it's an absolute URL that does not match our basename
        re(W.pathname, s) == null;
      }
      if (X) {
        F ? t.location.replace(S) : t.location.assign(S);
        return;
      }
    }
    N = null;
    let x = F === !0 || d.response.headers.has("X-Remix-Replace") ? "REPLACE" : "PUSH", { formMethod: D, formAction: k, formEncType: A } = f.navigation;
    !w && !R && D && k && A && (w = pr(f.navigation));
    let H = w || R;
    if (Aa.has(d.response.status) && H && ae(H.formMethod))
      await Re(x, M, {
        submission: {
          ...H,
          formAction: S
        },
        // Preserve these flags across redirects
        preventScrollReset: $ || j,
        enableViewTransition: m ? _ : void 0
      });
    else {
      let X = xt(
        M,
        w
      );
      await Re(x, M, {
        overrideNavigation: X,
        // Send fetcher submissions through for shouldRevalidate
        fetcherSubmission: R,
        // Preserve these flags across redirects
        preventScrollReset: $ || j,
        enableViewTransition: m ? _ : void 0
      });
    }
  }
  async function Oe(i, d, m, w, R, $) {
    let F, S = {};
    try {
      F = await Ga(
        u,
        i,
        d,
        m,
        w,
        $,
        a,
        n,
        R
      );
    } catch (M) {
      return m.forEach((x) => {
        S[x.route.id] = {
          type: "error",
          error: M
        };
      }), S;
    }
    for (let [M, x] of Object.entries(F))
      if (tn(x)) {
        let D = x.result;
        S[M] = {
          type: "redirect",
          response: Qa(
            D,
            d,
            M,
            w,
            s
          )
        };
      } else
        S[M] = await qa(
          x
        );
    return S;
  }
  async function It(i, d, m, w, R) {
    let $ = Oe(
      "loader",
      w,
      d,
      i,
      R,
      null
    ), F = Promise.all(
      m.map(async (x) => {
        if (x.matches && x.match && x.controller) {
          let k = (await Oe(
            "loader",
            Ue(e.history, x.path, x.controller.signal),
            [x.match],
            x.matches,
            R,
            x.key
          ))[x.match.route.id];
          return { [x.key]: k };
        } else
          return Promise.resolve({
            [x.key]: {
              type: "error",
              error: te(404, {
                pathname: x.path
              })
            }
          });
      })
    ), S = await $, M = (await F).reduce(
      (x, D) => Object.assign(x, D),
      {}
    );
    return {
      loaderResults: S,
      fetcherResults: M
    };
  }
  function vt() {
    ge = !0, Te.forEach((i, d) => {
      G.has(d) && qe.add(d), he(d);
    });
  }
  function fe(i, d, m = {}) {
    f.fetchers.set(i, d), Q(
      { fetchers: new Map(f.fetchers) },
      { flushSync: (m && m.flushSync) === !0 }
    );
  }
  function ce(i, d, m, w = {}) {
    let R = Pe(f.matches, d);
    yt(i), Q(
      {
        errors: {
          [R.route.id]: m
        },
        fetchers: new Map(f.fetchers)
      },
      { flushSync: (w && w.flushSync) === !0 }
    );
  }
  function jt(i) {
    return ze.set(i, (ze.get(i) || 0) + 1), ne.has(i) && ne.delete(i), f.fetchers.get(i) || _a;
  }
  function yt(i) {
    let d = f.fetchers.get(i);
    G.has(i) && !(d && d.state === "loading" && Me.has(i)) && he(i), Te.delete(i), Me.delete(i), ue.delete(i), ne.delete(i), qe.delete(i), f.fetchers.delete(i);
  }
  function Zr(i) {
    let d = (ze.get(i) || 0) - 1;
    d <= 0 ? (ze.delete(i), ne.add(i)) : ze.set(i, d), Q({ fetchers: new Map(f.fetchers) });
  }
  function he(i) {
    let d = G.get(i);
    d && (d.abort(), G.delete(i));
  }
  function Ht(i) {
    for (let d of i) {
      let m = jt(d), w = me(m.data);
      f.fetchers.set(d, w);
    }
  }
  function Bt() {
    let i = [], d = !1;
    for (let m of ue) {
      let w = f.fetchers.get(m);
      z(w, `Expected fetcher: ${m}`), w.state === "loading" && (ue.delete(m), i.push(m), d = !0);
    }
    return Ht(i), d;
  }
  function Wt(i) {
    let d = [];
    for (let [m, w] of Me)
      if (w < i) {
        let R = f.fetchers.get(m);
        z(R, `Expected fetcher: ${m}`), R.state === "loading" && (he(m), Me.delete(m), d.push(m));
      }
    return Ht(d), d.length > 0;
  }
  function ea(i, d) {
    let m = f.blockers.get(i) || je;
    return we.get(i) !== d && we.set(i, d), m;
  }
  function Vt(i) {
    f.blockers.delete(i), we.delete(i);
  }
  function et(i, d) {
    let m = f.blockers.get(i) || je;
    z(
      m.state === "unblocked" && d.state === "blocked" || m.state === "blocked" && d.state === "blocked" || m.state === "blocked" && d.state === "proceeding" || m.state === "blocked" && d.state === "unblocked" || m.state === "proceeding" && d.state === "unblocked",
      `Invalid blocker state transition: ${m.state} -> ${d.state}`
    );
    let w = new Map(f.blockers);
    w.set(i, d), Q({ blockers: w });
  }
  function Kt({
    currentLocation: i,
    nextLocation: d,
    historyAction: m
  }) {
    if (we.size === 0)
      return;
    we.size > 1 && K(!1, "A router only supports one blocker at a time");
    let w = Array.from(we.entries()), [R, $] = w[w.length - 1], F = f.blockers.get(R);
    if (!(F && F.state === "proceeding") && $({ currentLocation: i, nextLocation: d, historyAction: m }))
      return R;
  }
  function gt(i) {
    let d = te(404, { pathname: i }), m = l || o, { matches: w, route: R } = hr(m);
    return { notFoundMatches: w, route: R, error: d };
  }
  function ta(i, d, m) {
    if (g = i, E = d, y = m || null, !L && f.navigation === Et) {
      L = !0;
      let w = Jt(f.location, f.matches);
      w != null && Q({ restoreScrollPosition: w });
    }
    return () => {
      g = null, E = null, y = null;
    };
  }
  function Yt(i, d) {
    return y && y(
      i,
      d.map((w) => va(w, f.loaderData))
    ) || i.key;
  }
  function ra(i, d) {
    if (g && E) {
      let m = Yt(i, d);
      g[m] = E();
    }
  }
  function Jt(i, d) {
    if (g) {
      let m = Yt(i, d), w = g[m];
      if (typeof w == "number")
        return w;
    }
    return null;
  }
  function tt(i, d, m) {
    if (e.patchRoutesOnNavigation)
      if (i) {
        if (Object.keys(i[0].params).length > 0)
          return { active: !0, matches: st(
            d,
            m,
            s,
            !0
          ) };
      } else
        return { active: !0, matches: st(
          d,
          m,
          s,
          !0
        ) || [] };
    return { active: !1, matches: null };
  }
  async function rt(i, d, m, w) {
    if (!e.patchRoutesOnNavigation)
      return { type: "success", matches: i };
    let R = i;
    for (; ; ) {
      let $ = l == null, F = l || o, S = a;
      try {
        await e.patchRoutesOnNavigation({
          signal: m,
          path: d,
          matches: R,
          fetcherKey: w,
          patch: (D, k) => {
            m.aborted || nr(
              D,
              k,
              F,
              S,
              n
            );
          }
        });
      } catch (D) {
        return { type: "error", error: D, partialMatches: R };
      } finally {
        $ && !m.aborted && (o = [...o]);
      }
      if (m.aborted)
        return { type: "aborted" };
      let M = pe(F, d, s);
      if (M)
        return { type: "success", matches: M };
      let x = st(
        F,
        d,
        s,
        !0
      );
      if (!x || R.length === x.length && R.every(
        (D, k) => D.route.id === x[k].route.id
      ))
        return { type: "success", matches: null };
      R = x;
    }
  }
  function aa(i) {
    a = {}, l = dt(
      i,
      n,
      void 0,
      a
    );
  }
  function na(i, d) {
    let m = l == null;
    nr(
      i,
      d,
      l || o,
      a,
      n
    ), m && (o = [...o], Q({}));
  }
  return O = {
    get basename() {
      return s;
    },
    get future() {
      return c;
    },
    get state() {
      return f;
    },
    get routes() {
      return o;
    },
    get window() {
      return t;
    },
    initialize: Br,
    subscribe: Vr,
    enableScrollRestoration: ta,
    navigate: At,
    fetch: Xr,
    revalidate: Kr,
    // Passthrough to history-aware createHref used by useHref so we get proper
    // hash-aware URLs in DOM paths
    createHref: (i) => e.history.createHref(i),
    encodeLocation: (i) => e.history.encodeLocation(i),
    getFetcher: jt,
    deleteFetcher: Zr,
    dispose: Wr,
    getBlocker: ea,
    deleteBlocker: Vt,
    patchRoutes: na,
    _internalFetchControllers: G,
    // TODO: Remove setRoutes, it's temporary to avoid dealing with
    // updating the tree while validating the update algorithm.
    _internalSetRoutes: aa
  }, O;
}
function Ha(e) {
  return e != null && ("formData" in e && e.formData != null || "body" in e && e.body !== void 0);
}
function Lt(e, t, r, n, a, o) {
  let l, s;
  if (a) {
    l = [];
    for (let c of t)
      if (l.push(c), c.route.id === a) {
        s = c;
        break;
      }
  } else
    l = t, s = t[t.length - 1];
  let u = Mt(
    n || ".",
    Dt(l),
    re(e.pathname, r) || e.pathname,
    o === "path"
  );
  if (n == null && (u.search = e.search, u.hash = e.hash), (n == null || n === "" || n === ".") && s) {
    let c = Ft(u.search);
    if (s.route.index && !c)
      u.search = u.search ? u.search.replace(/^\?/, "?index&") : "?index";
    else if (!s.route.index && c) {
      let h = new URLSearchParams(u.search), v = h.getAll("index");
      h.delete("index"), v.filter((y) => y).forEach((y) => h.append("index", y));
      let g = h.toString();
      u.search = g ? `?${g}` : "";
    }
  }
  return r !== "/" && (u.pathname = u.pathname === "/" ? r : ie([r, u.pathname])), ve(u);
}
function er(e, t, r) {
  if (!r || !Ha(r))
    return { path: t };
  if (r.formMethod && !rn(r.formMethod))
    return {
      path: t,
      error: te(405, { method: r.formMethod })
    };
  let n = () => ({
    path: t,
    error: te(400, { type: "invalid-body" })
  }), o = (r.formMethod || "get").toUpperCase(), l = Fr(t);
  if (r.body !== void 0) {
    if (r.formEncType === "text/plain") {
      if (!ae(o))
        return n();
      let v = typeof r.body == "string" ? r.body : r.body instanceof FormData || r.body instanceof URLSearchParams ? (
        // https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#plain-text-form-data
        Array.from(r.body.entries()).reduce(
          (g, [y, E]) => `${g}${y}=${E}
`,
          ""
        )
      ) : String(r.body);
      return {
        path: t,
        submission: {
          formMethod: o,
          formAction: l,
          formEncType: r.formEncType,
          formData: void 0,
          json: void 0,
          text: v
        }
      };
    } else if (r.formEncType === "application/json") {
      if (!ae(o))
        return n();
      try {
        let v = typeof r.body == "string" ? JSON.parse(r.body) : r.body;
        return {
          path: t,
          submission: {
            formMethod: o,
            formAction: l,
            formEncType: r.formEncType,
            formData: void 0,
            json: v,
            text: void 0
          }
        };
      } catch {
        return n();
      }
    }
  }
  z(
    typeof FormData == "function",
    "FormData is not available in this environment"
  );
  let s, u;
  if (r.formData)
    s = St(r.formData), u = r.formData;
  else if (r.body instanceof FormData)
    s = St(r.body), u = r.body;
  else if (r.body instanceof URLSearchParams)
    s = r.body, u = ur(s);
  else if (r.body == null)
    s = new URLSearchParams(), u = new FormData();
  else
    try {
      s = new URLSearchParams(r.body), u = ur(s);
    } catch {
      return n();
    }
  let c = {
    formMethod: o,
    formAction: l,
    formEncType: r && r.formEncType || "application/x-www-form-urlencoded",
    formData: u,
    json: void 0,
    text: void 0
  };
  if (ae(c.formMethod))
    return { path: t, submission: c };
  let h = ye(t);
  return e && h.search && Ft(h.search) && s.append("index", ""), h.search = `?${s}`, { path: ve(h), submission: c };
}
function tr(e, t, r = !1) {
  let n = e.findIndex((a) => a.route.id === t);
  return n >= 0 ? e.slice(0, r ? n + 1 : n) : e;
}
function rr(e, t, r, n, a, o, l, s, u, c, h, v, g, y) {
  let E = y ? Z(y[1]) ? y[1].error : y[1].data : void 0, L = e.createURL(t.location), b = e.createURL(a), P = r;
  o && t.errors ? P = tr(
    r,
    Object.keys(t.errors)[0],
    !0
  ) : y && Z(y[1]) && (P = tr(
    r,
    y[0]
  ));
  let C = y ? y[1].statusCode : void 0, T = C && C >= 400, O = P.filter((U, j) => {
    let { route: N } = U;
    if (N.lazy)
      return !0;
    if (N.loader == null)
      return !1;
    if (o)
      return Ct(N, t.loaderData, t.errors);
    if (Ba(t.loaderData, t.matches[j], U))
      return !0;
    let _ = t.matches[j], Y = U;
    return ar(U, {
      currentUrl: L,
      currentParams: _.params,
      nextUrl: b,
      nextParams: Y.params,
      ...n,
      actionResult: E,
      actionStatus: C,
      defaultShouldRevalidate: T ? !1 : (
        // Forced revalidation due to submission, useRevalidator, or X-Remix-Revalidate
        l || L.pathname + L.search === b.pathname + b.search || // Search params affect all loaders
        L.search !== b.search || Wa(_, Y)
      )
    });
  }), f = [];
  return c.forEach((U, j) => {
    if (o || !r.some((De) => De.route.id === U.routeId) || u.has(j))
      return;
    let N = pe(v, U.path, g);
    if (!N) {
      f.push({
        key: j,
        routeId: U.routeId,
        path: U.path,
        matches: null,
        match: null,
        controller: null
      });
      return;
    }
    let _ = t.fetchers.get(j), Y = Ve(N, U.path), ee = !1;
    h.has(j) ? ee = !1 : s.has(j) ? (s.delete(j), ee = !0) : _ && _.state !== "idle" && _.data === void 0 ? ee = l : ee = ar(Y, {
      currentUrl: L,
      currentParams: t.matches[t.matches.length - 1].params,
      nextUrl: b,
      nextParams: r[r.length - 1].params,
      ...n,
      actionResult: E,
      actionStatus: C,
      defaultShouldRevalidate: T ? !1 : l
    }), ee && f.push({
      key: j,
      routeId: U.routeId,
      path: U.path,
      matches: N,
      match: Y,
      controller: new AbortController()
    });
  }), [O, f];
}
function Ct(e, t, r) {
  if (e.lazy)
    return !0;
  if (!e.loader)
    return !1;
  let n = t != null && t[e.id] !== void 0, a = r != null && r[e.id] !== void 0;
  return !n && a ? !1 : typeof e.loader == "function" && e.loader.hydrate === !0 ? !0 : !n && !a;
}
function Ba(e, t, r) {
  let n = (
    // [a] -> [a, b]
    !t || // [a, b] -> [a, c]
    r.route.id !== t.route.id
  ), a = !e.hasOwnProperty(r.route.id);
  return n || a;
}
function Wa(e, t) {
  let r = e.route.path;
  return (
    // param change for this match, /users/123 -> /users/456
    e.pathname !== t.pathname || // splat param changed, which is not present in match.path
    // e.g. /files/images/avatar.jpg -> files/finances.xls
    r != null && r.endsWith("*") && e.params["*"] !== t.params["*"]
  );
}
function ar(e, t) {
  if (e.route.shouldRevalidate) {
    let r = e.route.shouldRevalidate(t);
    if (typeof r == "boolean")
      return r;
  }
  return t.defaultShouldRevalidate;
}
function nr(e, t, r, n, a) {
  let o;
  if (e) {
    let u = n[e];
    z(
      u,
      `No route found to patch children into: routeId = ${e}`
    ), u.children || (u.children = []), o = u.children;
  } else
    o = r;
  let l = t.filter(
    (u) => !o.some(
      (c) => Sr(u, c)
    )
  ), s = dt(
    l,
    a,
    [e || "_", "patch", String((o == null ? void 0 : o.length) || "0")],
    n
  );
  o.push(...s);
}
function Sr(e, t) {
  return "id" in e && "id" in t && e.id === t.id ? !0 : e.index === t.index && e.path === t.path && e.caseSensitive === t.caseSensitive ? (!e.children || e.children.length === 0) && (!t.children || t.children.length === 0) ? !0 : e.children.every(
    (r, n) => {
      var a;
      return (a = t.children) == null ? void 0 : a.some((o) => Sr(r, o));
    }
  ) : !1;
}
var or = /* @__PURE__ */ new WeakMap(), Dr = ({
  key: e,
  route: t,
  manifest: r,
  mapRouteProperties: n
}) => {
  let a = r[t.id];
  if (z(a, "No route found in manifest"), !a.lazy || typeof a.lazy != "object")
    return;
  let o = a.lazy[e];
  if (!o)
    return;
  let l = or.get(a);
  l || (l = {}, or.set(a, l));
  let s = l[e];
  if (s)
    return s;
  let u = (async () => {
    let c = fa(e), v = a[e] !== void 0 && e !== "hasErrorBoundary";
    if (c)
      K(
        !c,
        "Route property " + e + " is not a supported lazy route property. This property will be ignored."
      ), l[e] = Promise.resolve();
    else if (v)
      K(
        !1,
        `Route "${a.id}" has a static property "${e}" defined. The lazy property will be ignored.`
      );
    else {
      let g = await o();
      g != null && (Object.assign(a, { [e]: g }), Object.assign(a, n(a)));
    }
    typeof a.lazy == "object" && (a.lazy[e] = void 0, Object.values(a.lazy).every((g) => g === void 0) && (a.lazy = void 0));
  })();
  return l[e] = u, u;
}, ir = /* @__PURE__ */ new WeakMap();
function Va(e, t, r, n) {
  let a = r[e.id];
  if (z(a, "No route found in manifest"), !e.lazy)
    return {
      lazyRoutePromise: void 0,
      lazyHandlerPromise: void 0
    };
  if (typeof e.lazy == "function") {
    let c = ir.get(a);
    if (c)
      return {
        lazyRoutePromise: c,
        lazyHandlerPromise: c
      };
    let h = (async () => {
      z(
        typeof e.lazy == "function",
        "No lazy route function found"
      );
      let v = await e.lazy(), g = {};
      for (let y in v) {
        let E = v[y];
        if (E === void 0)
          continue;
        let L = ma(y), P = a[y] !== void 0 && // This property isn't static since it should always be updated based
        // on the route updates
        y !== "hasErrorBoundary";
        L ? K(
          !L,
          "Route property " + y + " is not a supported property to be returned from a lazy route function. This property will be ignored."
        ) : P ? K(
          !P,
          `Route "${a.id}" has a static property "${y}" defined but its lazy function is also returning a value for this property. The lazy route property "${y}" will be ignored.`
        ) : g[y] = E;
      }
      Object.assign(a, g), Object.assign(a, {
        // To keep things framework agnostic, we use the provided `mapRouteProperties`
        // function to set the framework-aware properties (`element`/`hasErrorBoundary`)
        // since the logic will differ between frameworks.
        ...n(a),
        lazy: void 0
      });
    })();
    return ir.set(a, h), {
      lazyRoutePromise: h,
      lazyHandlerPromise: h
    };
  }
  let o = Object.keys(e.lazy), l = [], s;
  for (let c of o) {
    let h = Dr({
      key: c,
      route: e,
      manifest: r,
      mapRouteProperties: n
    });
    h && (l.push(h), c === t && (s = h));
  }
  return {
    lazyRoutePromise: Promise.all(l).then(() => {
    }),
    lazyHandlerPromise: s
  };
}
function Mr(e) {
  return e !== void 0;
}
function Ka(e, t, r) {
  let n = e.map(({ route: a }) => {
    if (!(typeof a.lazy != "object" || !a.lazy.unstable_middleware))
      return Dr({
        key: "unstable_middleware",
        route: a,
        manifest: t,
        mapRouteProperties: r
      });
  }).filter(Mr);
  return n.length > 0 ? Promise.all(n) : void 0;
}
async function lr(e) {
  let t = e.matches.filter((a) => a.shouldLoad), r = {};
  return (await Promise.all(t.map((a) => a.resolve()))).forEach((a, o) => {
    r[t[o].route.id] = a;
  }), r;
}
async function Ya(e) {
  return e.matches.some((t) => t.route.unstable_middleware) ? Ja(
    e,
    !1,
    () => lr(e),
    (t, r) => ({ [r]: { type: "error", result: t } })
  ) : lr(e);
}
async function Ja(e, t, r, n) {
  let { matches: a, request: o, params: l, context: s } = e, u = {
    handlerResult: void 0
  };
  try {
    let c = a.flatMap(
      (v) => v.route.unstable_middleware ? v.route.unstable_middleware.map((g) => [v.route.id, g]) : []
    ), h = await Tr(
      { request: o, params: l, context: s },
      c,
      t,
      u,
      r
    );
    return t ? h : u.handlerResult;
  } catch (c) {
    if (!u.middlewareError)
      throw c;
    let h = await n(
      u.middlewareError.error,
      u.middlewareError.routeId
    );
    return u.handlerResult ? Object.assign(u.handlerResult, h) : h;
  }
}
async function Tr(e, t, r, n, a, o = 0) {
  let { request: l } = e;
  if (l.signal.aborted)
    throw l.signal.reason ? l.signal.reason : new Error(
      `Request aborted without an \`AbortSignal.reason\`: ${l.method} ${l.url}`
    );
  let s = t[o];
  if (!s)
    return n.handlerResult = await a(), n.handlerResult;
  let [u, c] = s, h = !1, v, g = async () => {
    if (h)
      throw new Error("You may only call `next()` once per middleware");
    h = !0, await Tr(
      e,
      t,
      r,
      n,
      a,
      o + 1
    );
  };
  try {
    let y = await c(
      {
        request: e.request,
        params: e.params,
        context: e.context
      },
      g
    );
    return h ? y === void 0 ? v : y : g();
  } catch (y) {
    throw n.middlewareError ? n.middlewareError.error !== y && (n.middlewareError = { routeId: u, error: y }) : n.middlewareError = { routeId: u, error: y }, y;
  }
}
async function Ga(e, t, r, n, a, o, l, s, u) {
  let c = Ka(
    a,
    l,
    s
  ), h = a.map(
    (E) => Va(E.route, t, l, s)
  );
  c && await c;
  let v = a.map((E, L) => {
    let { lazyRoutePromise: b, lazyHandlerPromise: P } = h[L], C = n.some((O) => O.route.id === E.route.id);
    return {
      ...E,
      shouldLoad: C,
      resolve: async (O) => (O && r.method === "GET" && (E.route.lazy || E.route.loader) && (C = !0), C ? Xa({
        type: t,
        request: r,
        match: E,
        lazyHandlerPromise: P,
        lazyRoutePromise: b,
        handlerOverride: O,
        scopedContext: u
      }) : Promise.resolve({ type: "data", result: void 0 }))
    };
  }), g = await e({
    matches: v,
    request: r,
    params: a[0].params,
    fetcherKey: o,
    context: u
  }), y = h.flatMap(
    (E) => Object.values(E).filter(Mr)
  );
  try {
    await Promise.all(y);
  } catch {
  }
  return g;
}
async function Xa({
  type: e,
  request: t,
  match: r,
  lazyHandlerPromise: n,
  lazyRoutePromise: a,
  handlerOverride: o,
  scopedContext: l
}) {
  let s, u, c = (h) => {
    let v, g = new Promise((L, b) => v = b);
    u = () => v(), t.signal.addEventListener("abort", u);
    let y = (L) => typeof h != "function" ? Promise.reject(
      new Error(
        `You cannot call the handler for a route which defines a boolean "${e}" [routeId: ${r.route.id}]`
      )
    ) : h(
      {
        request: t,
        params: r.params,
        context: l
      },
      ...L !== void 0 ? [L] : []
    ), E = (async () => {
      try {
        return { type: "data", result: await (o ? o((b) => y(b)) : y()) };
      } catch (L) {
        return { type: "error", result: L };
      }
    })();
    return Promise.race([E, g]);
  };
  try {
    let h = r.route[e];
    if (n || a)
      if (h) {
        let v, [g] = await Promise.all([
          // If the handler throws, don't let it immediately bubble out,
          // since we need to let the lazy() execution finish so we know if this
          // route has a boundary that can handle the error
          c(h).catch((y) => {
            v = y;
          }),
          // Ensure all lazy route promises are resolved before continuing
          n,
          a
        ]);
        if (v !== void 0)
          throw v;
        s = g;
      } else if (await n, h = r.route[e], h)
        [s] = await Promise.all([c(h), a]);
      else if (e === "action") {
        let v = new URL(t.url), g = v.pathname + v.search;
        throw te(405, {
          method: t.method,
          pathname: g,
          routeId: r.route.id
        });
      } else
        return { type: "data", result: void 0 };
    else if (h)
      s = await c(h);
    else {
      let v = new URL(t.url), g = v.pathname + v.search;
      throw te(404, {
        pathname: g
      });
    }
  } catch (h) {
    return { type: "error", result: h };
  } finally {
    u && t.signal.removeEventListener("abort", u);
  }
  return s;
}
async function qa(e) {
  var n, a, o, l, s, u;
  let { result: t, type: r } = e;
  if ($r(t)) {
    let c;
    try {
      let h = t.headers.get("Content-Type");
      h && /\bapplication\/json\b/.test(h) ? t.body == null ? c = null : c = await t.json() : c = await t.text();
    } catch (h) {
      return { type: "error", error: h };
    }
    return r === "error" ? {
      type: "error",
      error: new ht(t.status, t.statusText, c),
      statusCode: t.status,
      headers: t.headers
    } : {
      type: "data",
      data: c,
      statusCode: t.status,
      headers: t.headers
    };
  }
  return r === "error" ? mr(t) ? t.data instanceof Error ? {
    type: "error",
    error: t.data,
    statusCode: (n = t.init) == null ? void 0 : n.status,
    headers: (a = t.init) != null && a.headers ? new Headers(t.init.headers) : void 0
  } : {
    type: "error",
    error: new ht(
      ((o = t.init) == null ? void 0 : o.status) || 500,
      void 0,
      t.data
    ),
    statusCode: Ye(t) ? t.status : void 0,
    headers: (l = t.init) != null && l.headers ? new Headers(t.init.headers) : void 0
  } : {
    type: "error",
    error: t,
    statusCode: Ye(t) ? t.status : void 0
  } : mr(t) ? {
    type: "data",
    data: t.data,
    statusCode: (s = t.init) == null ? void 0 : s.status,
    headers: (u = t.init) != null && u.headers ? new Headers(t.init.headers) : void 0
  } : { type: "data", data: t };
}
function Qa(e, t, r, n, a) {
  let o = e.headers.get("Location");
  if (z(
    o,
    "Redirects returned/thrown from loaders/actions must have a Location header"
  ), !Tt.test(o)) {
    let l = n.slice(
      0,
      n.findIndex((s) => s.route.id === r) + 1
    );
    o = Lt(
      new URL(t.url),
      l,
      a,
      o
    ), e.headers.set("Location", o);
  }
  return e;
}
function sr(e, t, r) {
  if (Tt.test(e)) {
    let n = e, a = n.startsWith("//") ? new URL(t.protocol + n) : new URL(n), o = re(a.pathname, r) != null;
    if (a.origin === t.origin && o)
      return a.pathname + a.search + a.hash;
  }
  return e;
}
function Ue(e, t, r, n) {
  let a = e.createURL(Fr(t)).toString(), o = { signal: r };
  if (n && ae(n.formMethod)) {
    let { formMethod: l, formEncType: s } = n;
    o.method = l.toUpperCase(), s === "application/json" ? (o.headers = new Headers({ "Content-Type": s }), o.body = JSON.stringify(n.json)) : s === "text/plain" ? o.body = n.text : s === "application/x-www-form-urlencoded" && n.formData ? o.body = St(n.formData) : o.body = n.formData;
  }
  return new Request(a, o);
}
function St(e) {
  let t = new URLSearchParams();
  for (let [r, n] of e.entries())
    t.append(r, typeof n == "string" ? n : n.name);
  return t;
}
function ur(e) {
  let t = new FormData();
  for (let [r, n] of e.entries())
    t.append(r, n);
  return t;
}
function Za(e, t, r, n = !1, a = !1) {
  let o = {}, l = null, s, u = !1, c = {}, h = r && Z(r[1]) ? r[1].error : void 0;
  return e.forEach((v) => {
    if (!(v.route.id in t))
      return;
    let g = v.route.id, y = t[g];
    if (z(
      !Le(y),
      "Cannot handle redirect results in processLoaderData"
    ), Z(y)) {
      let E = y.error;
      if (h !== void 0 && (E = h, h = void 0), l = l || {}, a)
        l[g] = E;
      else {
        let L = Pe(e, g);
        l[L.route.id] == null && (l[L.route.id] = E);
      }
      n || (o[g] = Cr), u || (u = !0, s = Ye(y.error) ? y.error.status : 500), y.headers && (c[g] = y.headers);
    } else
      o[g] = y.data, y.statusCode && y.statusCode !== 200 && !u && (s = y.statusCode), y.headers && (c[g] = y.headers);
  }), h !== void 0 && r && (l = { [r[0]]: h }, o[r[0]] = void 0), {
    loaderData: o,
    errors: l,
    statusCode: s || 200,
    loaderHeaders: c
  };
}
function cr(e, t, r, n, a, o) {
  let { loaderData: l, errors: s } = Za(
    t,
    r,
    n
  );
  return a.forEach((u) => {
    let { key: c, match: h, controller: v } = u, g = o[c];
    if (z(g, "Did not find corresponding fetcher result"), !(v && v.signal.aborted))
      if (Z(g)) {
        let y = Pe(e.matches, h == null ? void 0 : h.route.id);
        s && s[y.route.id] || (s = {
          ...s,
          [y.route.id]: g.error
        }), e.fetchers.delete(c);
      } else if (Le(g))
        z(!1, "Unhandled fetcher revalidation redirect");
      else {
        let y = me(g.data);
        e.fetchers.set(c, y);
      }
  }), { loaderData: l, errors: s };
}
function dr(e, t, r, n) {
  let a = Object.entries(t).filter(([, o]) => o !== Cr).reduce((o, [l, s]) => (o[l] = s, o), {});
  for (let o of r) {
    let l = o.route.id;
    if (!t.hasOwnProperty(l) && e.hasOwnProperty(l) && o.route.loader && (a[l] = e[l]), n && n.hasOwnProperty(l))
      break;
  }
  return a;
}
function fr(e) {
  return e ? Z(e[1]) ? {
    // Clear out prior actionData on errors
    actionData: {}
  } : {
    actionData: {
      [e[0]]: e[1].data
    }
  } : {};
}
function Pe(e, t) {
  return (t ? e.slice(0, e.findIndex((n) => n.route.id === t) + 1) : [...e]).reverse().find((n) => n.route.hasErrorBoundary === !0) || e[0];
}
function hr(e) {
  let t = e.length === 1 ? e[0] : e.find((r) => r.index || !r.path || r.path === "/") || {
    id: "__shim-error-route__"
  };
  return {
    matches: [
      {
        params: {},
        pathname: "",
        pathnameBase: "",
        route: t
      }
    ],
    route: t
  };
}
function te(e, {
  pathname: t,
  routeId: r,
  method: n,
  type: a,
  message: o
} = {}) {
  let l = "Unknown Server Error", s = "Unknown @remix-run/router error";
  return e === 400 ? (l = "Bad Request", n && t && r ? s = `You made a ${n} request to "${t}" but did not provide a \`loader\` for route "${r}", so there is no way to handle the request.` : a === "invalid-body" && (s = "Unable to encode submission body")) : e === 403 ? (l = "Forbidden", s = `Route "${r}" does not match URL "${t}"`) : e === 404 ? (l = "Not Found", s = `No route matches URL "${t}"`) : e === 405 && (l = "Method Not Allowed", n && t && r ? s = `You made a ${n.toUpperCase()} request to "${t}" but did not provide an \`action\` for route "${r}", so there is no way to handle the request.` : n && (s = `Invalid request method "${n.toUpperCase()}"`)), new ht(
    e || 500,
    l,
    new Error(s),
    !0
  );
}
function it(e) {
  let t = Object.entries(e);
  for (let r = t.length - 1; r >= 0; r--) {
    let [n, a] = t[r];
    if (Le(a))
      return { key: n, result: a };
  }
}
function Fr(e) {
  let t = typeof e == "string" ? ye(e) : e;
  return ve({ ...t, hash: "" });
}
function en(e, t) {
  return e.pathname !== t.pathname || e.search !== t.search ? !1 : e.hash === "" ? t.hash !== "" : e.hash === t.hash ? !0 : t.hash !== "";
}
function tn(e) {
  return $r(e.result) && Oa.has(e.result.status);
}
function Z(e) {
  return e.type === "error";
}
function Le(e) {
  return (e && e.type) === "redirect";
}
function mr(e) {
  return typeof e == "object" && e != null && "type" in e && "data" in e && "init" in e && e.type === "DataWithResponseInit";
}
function $r(e) {
  return e != null && typeof e.status == "number" && typeof e.statusText == "string" && typeof e.headers == "object" && typeof e.body < "u";
}
function rn(e) {
  return za.has(e.toUpperCase());
}
function ae(e) {
  return Ua.has(e.toUpperCase());
}
function Ft(e) {
  return new URLSearchParams(e).getAll("index").some((t) => t === "");
}
function Ve(e, t) {
  let r = typeof t == "string" ? ye(t).search : t.search;
  if (e[e.length - 1].route.index && Ft(r || ""))
    return e[e.length - 1];
  let n = xr(e);
  return n[n.length - 1];
}
function pr(e) {
  let { formMethod: t, formAction: r, formEncType: n, text: a, formData: o, json: l } = e;
  if (!(!t || !r || !n)) {
    if (a != null)
      return {
        formMethod: t,
        formAction: r,
        formEncType: n,
        formData: void 0,
        json: void 0,
        text: a
      };
    if (o != null)
      return {
        formMethod: t,
        formAction: r,
        formEncType: n,
        formData: o,
        json: void 0,
        text: void 0
      };
    if (l !== void 0)
      return {
        formMethod: t,
        formAction: r,
        formEncType: n,
        formData: void 0,
        json: l,
        text: void 0
      };
  }
}
function xt(e, t) {
  return t ? {
    state: "loading",
    location: e,
    formMethod: t.formMethod,
    formAction: t.formAction,
    formEncType: t.formEncType,
    formData: t.formData,
    json: t.json,
    text: t.text
  } : {
    state: "loading",
    location: e,
    formMethod: void 0,
    formAction: void 0,
    formEncType: void 0,
    formData: void 0,
    json: void 0,
    text: void 0
  };
}
function an(e, t) {
  return {
    state: "submitting",
    location: e,
    formMethod: t.formMethod,
    formAction: t.formAction,
    formEncType: t.formEncType,
    formData: t.formData,
    json: t.json,
    text: t.text
  };
}
function He(e, t) {
  return e ? {
    state: "loading",
    formMethod: e.formMethod,
    formAction: e.formAction,
    formEncType: e.formEncType,
    formData: e.formData,
    json: e.json,
    text: e.text,
    data: t
  } : {
    state: "loading",
    formMethod: void 0,
    formAction: void 0,
    formEncType: void 0,
    formData: void 0,
    json: void 0,
    text: void 0,
    data: t
  };
}
function nn(e, t) {
  return {
    state: "submitting",
    formMethod: e.formMethod,
    formAction: e.formAction,
    formEncType: e.formEncType,
    formData: e.formData,
    json: e.json,
    text: e.text,
    data: t ? t.data : void 0
  };
}
function me(e) {
  return {
    state: "idle",
    formMethod: void 0,
    formAction: void 0,
    formEncType: void 0,
    formData: void 0,
    json: void 0,
    text: void 0,
    data: e
  };
}
function on(e, t) {
  try {
    let r = e.sessionStorage.getItem(
      Lr
    );
    if (r) {
      let n = JSON.parse(r);
      for (let [a, o] of Object.entries(n || {}))
        o && Array.isArray(o) && t.set(a, new Set(o || []));
    }
  } catch {
  }
}
function ln(e, t) {
  if (t.size > 0) {
    let r = {};
    for (let [n, a] of t)
      r[n] = [...a];
    try {
      e.sessionStorage.setItem(
        Lr,
        JSON.stringify(r)
      );
    } catch (n) {
      K(
        !1,
        `Failed to save applied view transitions in sessionStorage (${n}).`
      );
    }
  }
}
function sn() {
  let e, t, r = new Promise((n, a) => {
    e = async (o) => {
      n(o);
      try {
        await r;
      } catch {
      }
    }, t = async (o) => {
      a(o);
      try {
        await r;
      } catch {
      }
    };
  });
  return {
    promise: r,
    //@ts-ignore
    resolve: e,
    //@ts-ignore
    reject: t
  };
}
var Ce = p.createContext(null);
Ce.displayName = "DataRouter";
var Je = p.createContext(null);
Je.displayName = "DataRouterState";
var $t = p.createContext({
  isTransitioning: !1
});
$t.displayName = "ViewTransition";
var kr = p.createContext(
  /* @__PURE__ */ new Map()
);
kr.displayName = "Fetchers";
var un = p.createContext(null);
un.displayName = "Await";
var le = p.createContext(
  null
);
le.displayName = "Navigation";
var mt = p.createContext(
  null
);
mt.displayName = "Location";
var se = p.createContext({
  outlet: null,
  matches: [],
  isDataRoute: !1
});
se.displayName = "Route";
var kt = p.createContext(null);
kt.displayName = "RouteError";
function cn(e, { relative: t } = {}) {
  z(
    Ge(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useHref() may be used only in the context of a <Router> component."
  );
  let { basename: r, navigator: n } = p.useContext(le), { hash: a, pathname: o, search: l } = Xe(e, { relative: t }), s = o;
  return r !== "/" && (s = o === "/" ? r : ie([r, o])), n.createHref({ pathname: s, search: l, hash: a });
}
function Ge() {
  return p.useContext(mt) != null;
}
function Se() {
  return z(
    Ge(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useLocation() may be used only in the context of a <Router> component."
  ), p.useContext(mt).location;
}
var Ur = "You should call navigate() in a React.useEffect(), not when your component is first rendered.";
function Nr(e) {
  p.useContext(le).static || p.useLayoutEffect(e);
}
function dn() {
  let { isDataRoute: e } = p.useContext(se);
  return e ? Cn() : fn();
}
function fn() {
  z(
    Ge(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useNavigate() may be used only in the context of a <Router> component."
  );
  let e = p.useContext(Ce), { basename: t, navigator: r } = p.useContext(le), { matches: n } = p.useContext(se), { pathname: a } = Se(), o = JSON.stringify(Dt(n)), l = p.useRef(!1);
  return Nr(() => {
    l.current = !0;
  }), p.useCallback(
    (u, c = {}) => {
      if (K(l.current, Ur), !l.current) return;
      if (typeof u == "number") {
        r.go(u);
        return;
      }
      let h = Mt(
        u,
        JSON.parse(o),
        a,
        c.relative === "path"
      );
      e == null && t !== "/" && (h.pathname = h.pathname === "/" ? t : ie([t, h.pathname])), (c.replace ? r.replace : r.push)(
        h,
        c.state,
        c
      );
    },
    [
      t,
      r,
      o,
      a,
      e
    ]
  );
}
var hn = p.createContext(null);
function mn(e) {
  let t = p.useContext(se).outlet;
  return t && /* @__PURE__ */ p.createElement(hn.Provider, { value: e }, t);
}
function Xe(e, { relative: t } = {}) {
  let { matches: r } = p.useContext(se), { pathname: n } = Se(), a = JSON.stringify(Dt(r));
  return p.useMemo(
    () => Mt(
      e,
      JSON.parse(a),
      n,
      t === "path"
    ),
    [e, a, n, t]
  );
}
function pn(e, t, r, n) {
  z(
    Ge(),
    // TODO: This error is probably because they somehow have 2 versions of the
    // router loaded. We can help them understand how to avoid that.
    "useRoutes() may be used only in the context of a <Router> component."
  );
  let { navigator: a, static: o } = p.useContext(le), { matches: l } = p.useContext(se), s = l[l.length - 1], u = s ? s.params : {}, c = s ? s.pathname : "/", h = s ? s.pathnameBase : "/", v = s && s.route;
  {
    let C = v && v.path || "";
    zr(
      c,
      !v || C.endsWith("*") || C.endsWith("*?"),
      `You rendered descendant <Routes> (or called \`useRoutes()\`) at "${c}" (under <Route path="${C}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${C}"> to <Route path="${C === "/" ? "*" : `${C}/*`}">.`
    );
  }
  let g = Se(), y;
  y = g;
  let E = y.pathname || "/", L = E;
  if (h !== "/") {
    let C = h.replace(/^\//, "").split("/");
    L = "/" + E.replace(/^\//, "").split("/").slice(C.length).join("/");
  }
  let b = !o && r && r.matches && r.matches.length > 0 ? r.matches : pe(e, { pathname: L });
  return K(
    v || b != null,
    `No routes matched location "${y.pathname}${y.search}${y.hash}" `
  ), K(
    b == null || b[b.length - 1].route.element !== void 0 || b[b.length - 1].route.Component !== void 0 || b[b.length - 1].route.lazy !== void 0,
    `Matched leaf route at location "${y.pathname}${y.search}${y.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`
  ), bn(
    b && b.map(
      (C) => Object.assign({}, C, {
        params: Object.assign({}, u, C.params),
        pathname: ie([
          h,
          // Re-encode pathnames that were decoded inside matchRoutes
          a.encodeLocation ? a.encodeLocation(C.pathname).pathname : C.pathname
        ]),
        pathnameBase: C.pathnameBase === "/" ? h : ie([
          h,
          // Re-encode pathnames that were decoded inside matchRoutes
          a.encodeLocation ? a.encodeLocation(C.pathnameBase).pathname : C.pathnameBase
        ])
      })
    ),
    l,
    r,
    n
  );
}
function vn() {
  let e = Ln(), t = Ye(e) ? `${e.status} ${e.statusText}` : e instanceof Error ? e.message : JSON.stringify(e), r = e instanceof Error ? e.stack : null, n = "rgba(200,200,200, 0.5)", a = { padding: "0.5rem", backgroundColor: n }, o = { padding: "2px 4px", backgroundColor: n }, l = null;
  return console.error(
    "Error handled by React Router default ErrorBoundary:",
    e
  ), l = /* @__PURE__ */ p.createElement(p.Fragment, null, /* @__PURE__ */ p.createElement("p", null, "💿 Hey developer 👋"), /* @__PURE__ */ p.createElement("p", null, "You can provide a way better UX than this when your app throws errors by providing your own ", /* @__PURE__ */ p.createElement("code", { style: o }, "ErrorBoundary"), " or", " ", /* @__PURE__ */ p.createElement("code", { style: o }, "errorElement"), " prop on your route.")), /* @__PURE__ */ p.createElement(p.Fragment, null, /* @__PURE__ */ p.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ p.createElement("h3", { style: { fontStyle: "italic" } }, t), r ? /* @__PURE__ */ p.createElement("pre", { style: a }, r) : null, l);
}
var yn = /* @__PURE__ */ p.createElement(vn, null), gn = class extends p.Component {
  constructor(e) {
    super(e), this.state = {
      location: e.location,
      revalidation: e.revalidation,
      error: e.error
    };
  }
  static getDerivedStateFromError(e) {
    return { error: e };
  }
  static getDerivedStateFromProps(e, t) {
    return t.location !== e.location || t.revalidation !== "idle" && e.revalidation === "idle" ? {
      error: e.error,
      location: e.location,
      revalidation: e.revalidation
    } : {
      error: e.error !== void 0 ? e.error : t.error,
      location: t.location,
      revalidation: e.revalidation || t.revalidation
    };
  }
  componentDidCatch(e, t) {
    console.error(
      "React Router caught the following error during render",
      e,
      t
    );
  }
  render() {
    return this.state.error !== void 0 ? /* @__PURE__ */ p.createElement(se.Provider, { value: this.props.routeContext }, /* @__PURE__ */ p.createElement(
      kt.Provider,
      {
        value: this.state.error,
        children: this.props.component
      }
    )) : this.props.children;
  }
};
function wn({ routeContext: e, match: t, children: r }) {
  let n = p.useContext(Ce);
  return n && n.static && n.staticContext && (t.route.errorElement || t.route.ErrorBoundary) && (n.staticContext._deepestRenderedBoundaryId = t.route.id), /* @__PURE__ */ p.createElement(se.Provider, { value: e }, r);
}
function bn(e, t = [], r = null, n = null) {
  if (e == null) {
    if (!r)
      return null;
    if (r.errors)
      e = r.matches;
    else if (t.length === 0 && !r.initialized && r.matches.length > 0)
      e = r.matches;
    else
      return null;
  }
  let a = e, o = r == null ? void 0 : r.errors;
  if (o != null) {
    let u = a.findIndex(
      (c) => c.route.id && (o == null ? void 0 : o[c.route.id]) !== void 0
    );
    z(
      u >= 0,
      `Could not find a matching route for errors on route IDs: ${Object.keys(
        o
      ).join(",")}`
    ), a = a.slice(
      0,
      Math.min(a.length, u + 1)
    );
  }
  let l = !1, s = -1;
  if (r)
    for (let u = 0; u < a.length; u++) {
      let c = a[u];
      if ((c.route.HydrateFallback || c.route.hydrateFallbackElement) && (s = u), c.route.id) {
        let { loaderData: h, errors: v } = r, g = c.route.loader && !h.hasOwnProperty(c.route.id) && (!v || v[c.route.id] === void 0);
        if (c.route.lazy || g) {
          l = !0, s >= 0 ? a = a.slice(0, s + 1) : a = [a[0]];
          break;
        }
      }
    }
  return a.reduceRight((u, c, h) => {
    let v, g = !1, y = null, E = null;
    r && (v = o && c.route.id ? o[c.route.id] : void 0, y = c.route.errorElement || yn, l && (s < 0 && h === 0 ? (zr(
      "route-fallback",
      !1,
      "No `HydrateFallback` element provided to render during initial hydration"
    ), g = !0, E = null) : s === h && (g = !0, E = c.route.hydrateFallbackElement || null)));
    let L = t.concat(a.slice(0, h + 1)), b = () => {
      let P;
      return v ? P = y : g ? P = E : c.route.Component ? P = /* @__PURE__ */ p.createElement(c.route.Component, null) : c.route.element ? P = c.route.element : P = u, /* @__PURE__ */ p.createElement(
        wn,
        {
          match: c,
          routeContext: {
            outlet: u,
            matches: L,
            isDataRoute: r != null
          },
          children: P
        }
      );
    };
    return r && (c.route.ErrorBoundary || c.route.errorElement || h === 0) ? /* @__PURE__ */ p.createElement(
      gn,
      {
        location: r.location,
        revalidation: r.revalidation,
        component: y,
        error: v,
        children: b(),
        routeContext: { outlet: null, matches: L, isDataRoute: !0 }
      }
    ) : b();
  }, null);
}
function Ut(e) {
  return `${e} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
}
function Rn(e) {
  let t = p.useContext(Ce);
  return z(t, Ut(e)), t;
}
function En(e) {
  let t = p.useContext(Je);
  return z(t, Ut(e)), t;
}
function xn(e) {
  let t = p.useContext(se);
  return z(t, Ut(e)), t;
}
function Nt(e) {
  let t = xn(e), r = t.matches[t.matches.length - 1];
  return z(
    r.route.id,
    `${e} can only be used on routes that contain a unique "id"`
  ), r.route.id;
}
function Pn() {
  return Nt(
    "useRouteId"
    /* UseRouteId */
  );
}
function Ln() {
  var n;
  let e = p.useContext(kt), t = En(
    "useRouteError"
    /* UseRouteError */
  ), r = Nt(
    "useRouteError"
    /* UseRouteError */
  );
  return e !== void 0 ? e : (n = t.errors) == null ? void 0 : n[r];
}
function Cn() {
  let { router: e } = Rn(
    "useNavigate"
    /* UseNavigateStable */
  ), t = Nt(
    "useNavigate"
    /* UseNavigateStable */
  ), r = p.useRef(!1);
  return Nr(() => {
    r.current = !0;
  }), p.useCallback(
    async (a, o = {}) => {
      K(r.current, Ur), r.current && (typeof a == "number" ? e.navigate(a) : await e.navigate(a, { fromRouteId: t, ...o }));
    },
    [e, t]
  );
}
var vr = {};
function zr(e, t, r) {
  !t && !vr[e] && (vr[e] = !0, K(!1, r));
}
var yr = {};
function gr(e, t) {
  !e && !yr[t] && (yr[t] = !0, console.warn(t));
}
function Sn(e) {
  let t = {
    // Note: this check also occurs in createRoutesFromChildren so update
    // there if you change this -- please and thank you!
    hasErrorBoundary: e.hasErrorBoundary || e.ErrorBoundary != null || e.errorElement != null
  };
  return e.Component && (e.element && K(
    !1,
    "You should not include both `Component` and `element` on your route - `Component` will be used."
  ), Object.assign(t, {
    element: p.createElement(e.Component),
    Component: void 0
  })), e.HydrateFallback && (e.hydrateFallbackElement && K(
    !1,
    "You should not include both `HydrateFallback` and `hydrateFallbackElement` on your route - `HydrateFallback` will be used."
  ), Object.assign(t, {
    hydrateFallbackElement: p.createElement(e.HydrateFallback),
    HydrateFallback: void 0
  })), e.ErrorBoundary && (e.errorElement && K(
    !1,
    "You should not include both `ErrorBoundary` and `errorElement` on your route - `ErrorBoundary` will be used."
  ), Object.assign(t, {
    errorElement: p.createElement(e.ErrorBoundary),
    ErrorBoundary: void 0
  })), t;
}
var Dn = class {
  constructor() {
    this.status = "pending", this.promise = new Promise((e, t) => {
      this.resolve = (r) => {
        this.status === "pending" && (this.status = "resolved", e(r));
      }, this.reject = (r) => {
        this.status === "pending" && (this.status = "rejected", t(r));
      };
    });
  }
};
function mo({
  router: e,
  flushSync: t
}) {
  let [r, n] = p.useState(e.state), [a, o] = p.useState(), [l, s] = p.useState({
    isTransitioning: !1
  }), [u, c] = p.useState(), [h, v] = p.useState(), [g, y] = p.useState(), E = p.useRef(/* @__PURE__ */ new Map()), L = p.useCallback(
    (T, { deletedFetchers: O, flushSync: f, viewTransitionOpts: U }) => {
      T.fetchers.forEach((N, _) => {
        N.data !== void 0 && E.current.set(_, N.data);
      }), O.forEach((N) => E.current.delete(N)), gr(
        f === !1 || t != null,
        'You provided the `flushSync` option to a router update, but you are not using the `<RouterProvider>` from `react-router/dom` so `ReactDOM.flushSync()` is unavailable.  Please update your app to `import { RouterProvider } from "react-router/dom"` and ensure you have `react-dom` installed as a dependency to use the `flushSync` option.'
      );
      let j = e.window != null && e.window.document != null && typeof e.window.document.startViewTransition == "function";
      if (gr(
        U == null || j,
        "You provided the `viewTransition` option to a router update, but you do not appear to be running in a DOM environment as `window.startViewTransition` is not available."
      ), !U || !j) {
        t && f ? t(() => n(T)) : p.startTransition(() => n(T));
        return;
      }
      if (t && f) {
        t(() => {
          h && (u && u.resolve(), h.skipTransition()), s({
            isTransitioning: !0,
            flushSync: !0,
            currentLocation: U.currentLocation,
            nextLocation: U.nextLocation
          });
        });
        let N = e.window.document.startViewTransition(() => {
          t(() => n(T));
        });
        N.finished.finally(() => {
          t(() => {
            c(void 0), v(void 0), o(void 0), s({ isTransitioning: !1 });
          });
        }), t(() => v(N));
        return;
      }
      h ? (u && u.resolve(), h.skipTransition(), y({
        state: T,
        currentLocation: U.currentLocation,
        nextLocation: U.nextLocation
      })) : (o(T), s({
        isTransitioning: !0,
        flushSync: !1,
        currentLocation: U.currentLocation,
        nextLocation: U.nextLocation
      }));
    },
    [e.window, t, h, u]
  );
  p.useLayoutEffect(() => e.subscribe(L), [e, L]), p.useEffect(() => {
    l.isTransitioning && !l.flushSync && c(new Dn());
  }, [l]), p.useEffect(() => {
    if (u && a && e.window) {
      let T = a, O = u.promise, f = e.window.document.startViewTransition(async () => {
        p.startTransition(() => n(T)), await O;
      });
      f.finished.finally(() => {
        c(void 0), v(void 0), o(void 0), s({ isTransitioning: !1 });
      }), v(f);
    }
  }, [a, u, e.window]), p.useEffect(() => {
    u && a && r.location.key === a.location.key && u.resolve();
  }, [u, h, r.location, a]), p.useEffect(() => {
    !l.isTransitioning && g && (o(g.state), s({
      isTransitioning: !0,
      flushSync: !1,
      currentLocation: g.currentLocation,
      nextLocation: g.nextLocation
    }), y(void 0));
  }, [l.isTransitioning, g]);
  let b = p.useMemo(() => ({
    createHref: e.createHref,
    encodeLocation: e.encodeLocation,
    go: (T) => e.navigate(T),
    push: (T, O, f) => e.navigate(T, {
      state: O,
      preventScrollReset: f == null ? void 0 : f.preventScrollReset
    }),
    replace: (T, O, f) => e.navigate(T, {
      replace: !0,
      state: O,
      preventScrollReset: f == null ? void 0 : f.preventScrollReset
    })
  }), [e]), P = e.basename || "/", C = p.useMemo(
    () => ({
      router: e,
      navigator: b,
      static: !1,
      basename: P
    }),
    [e, b, P]
  );
  return /* @__PURE__ */ p.createElement(p.Fragment, null, /* @__PURE__ */ p.createElement(Ce.Provider, { value: C }, /* @__PURE__ */ p.createElement(Je.Provider, { value: r }, /* @__PURE__ */ p.createElement(kr.Provider, { value: E.current }, /* @__PURE__ */ p.createElement($t.Provider, { value: l }, /* @__PURE__ */ p.createElement(
    Fn,
    {
      basename: P,
      location: r.location,
      navigationType: r.historyAction,
      navigator: b
    },
    /* @__PURE__ */ p.createElement(
      Mn,
      {
        routes: e.routes,
        future: e.future,
        state: r
      }
    )
  ))))), null);
}
var Mn = p.memo(Tn);
function Tn({
  routes: e,
  future: t,
  state: r
}) {
  return pn(e, void 0, r, t);
}
function po(e) {
  return mn(e.context);
}
function Fn({
  basename: e = "/",
  children: t = null,
  location: r,
  navigationType: n = "POP",
  navigator: a,
  static: o = !1
}) {
  z(
    !Ge(),
    "You cannot render a <Router> inside another <Router>. You should never have more than one in your app."
  );
  let l = e.replace(/^\/*/, "/"), s = p.useMemo(
    () => ({
      basename: l,
      navigator: a,
      static: o,
      future: {}
    }),
    [l, a, o]
  );
  typeof r == "string" && (r = ye(r));
  let {
    pathname: u = "/",
    search: c = "",
    hash: h = "",
    state: v = null,
    key: g = "default"
  } = r, y = p.useMemo(() => {
    let E = re(u, l);
    return E == null ? null : {
      location: {
        pathname: E,
        search: c,
        hash: h,
        state: v,
        key: g
      },
      navigationType: n
    };
  }, [l, u, c, h, v, g, n]);
  return K(
    y != null,
    `<Router basename="${l}"> is not able to match the URL "${u}${c}${h}" because it does not start with the basename, so the <Router> won't render anything.`
  ), y == null ? null : /* @__PURE__ */ p.createElement(le.Provider, { value: s }, /* @__PURE__ */ p.createElement(mt.Provider, { children: t, value: y }));
}
var ut = "get", ct = "application/x-www-form-urlencoded";
function pt(e) {
  return e != null && typeof e.tagName == "string";
}
function $n(e) {
  return pt(e) && e.tagName.toLowerCase() === "button";
}
function kn(e) {
  return pt(e) && e.tagName.toLowerCase() === "form";
}
function Un(e) {
  return pt(e) && e.tagName.toLowerCase() === "input";
}
function Nn(e) {
  return !!(e.metaKey || e.altKey || e.ctrlKey || e.shiftKey);
}
function zn(e, t) {
  return e.button === 0 && // Ignore everything but left clicks
  (!t || t === "_self") && // Let browser handle "target=_blank" etc.
  !Nn(e);
}
var lt = null;
function On() {
  if (lt === null)
    try {
      new FormData(
        document.createElement("form"),
        // @ts-expect-error if FormData supports the submitter parameter, this will throw
        0
      ), lt = !1;
    } catch {
      lt = !0;
    }
  return lt;
}
var An = /* @__PURE__ */ new Set([
  "application/x-www-form-urlencoded",
  "multipart/form-data",
  "text/plain"
]);
function Pt(e) {
  return e != null && !An.has(e) ? (K(
    !1,
    `"${e}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${ct}"`
  ), null) : e;
}
function _n(e, t) {
  let r, n, a, o, l;
  if (kn(e)) {
    let s = e.getAttribute("action");
    n = s ? re(s, t) : null, r = e.getAttribute("method") || ut, a = Pt(e.getAttribute("enctype")) || ct, o = new FormData(e);
  } else if ($n(e) || Un(e) && (e.type === "submit" || e.type === "image")) {
    let s = e.form;
    if (s == null)
      throw new Error(
        'Cannot submit a <button> or <input type="submit"> without a <form>'
      );
    let u = e.getAttribute("formaction") || s.getAttribute("action");
    if (n = u ? re(u, t) : null, r = e.getAttribute("formmethod") || s.getAttribute("method") || ut, a = Pt(e.getAttribute("formenctype")) || Pt(s.getAttribute("enctype")) || ct, o = new FormData(s, e), !On()) {
      let { name: c, type: h, value: v } = e;
      if (h === "image") {
        let g = c ? `${c}.` : "";
        o.append(`${g}x`, "0"), o.append(`${g}y`, "0");
      } else c && o.append(c, v);
    }
  } else {
    if (pt(e))
      throw new Error(
        'Cannot submit element that is not <form>, <button>, or <input type="submit|image">'
      );
    r = ut, n = null, a = ct, l = e;
  }
  return o && a === "text/plain" && (l = o, o = void 0), { action: n, method: r.toLowerCase(), encType: a, formData: o, body: l };
}
function zt(e, t) {
  if (e === !1 || e === null || typeof e > "u")
    throw new Error(t);
}
async function In(e, t) {
  if (e.id in t)
    return t[e.id];
  try {
    let r = await import(
      /* @vite-ignore */
      /* webpackIgnore: true */
      e.module
    );
    return t[e.id] = r, r;
  } catch (r) {
    return console.error(
      `Error loading route module \`${e.module}\`, reloading page...`
    ), console.error(r), window.__reactRouterContext && window.__reactRouterContext.isSpaMode, window.location.reload(), new Promise(() => {
    });
  }
}
function jn(e) {
  return e == null ? !1 : e.href == null ? e.rel === "preload" && typeof e.imageSrcSet == "string" && typeof e.imageSizes == "string" : typeof e.rel == "string" && typeof e.href == "string";
}
async function Hn(e, t, r) {
  let n = await Promise.all(
    e.map(async (a) => {
      let o = t.routes[a.route.id];
      if (o) {
        let l = await In(o, r);
        return l.links ? l.links() : [];
      }
      return [];
    })
  );
  return Kn(
    n.flat(1).filter(jn).filter((a) => a.rel === "stylesheet" || a.rel === "preload").map(
      (a) => a.rel === "stylesheet" ? { ...a, rel: "prefetch", as: "style" } : { ...a, rel: "prefetch" }
    )
  );
}
function wr(e, t, r, n, a, o) {
  let l = (u, c) => r[c] ? u.route.id !== r[c].route.id : !0, s = (u, c) => {
    var h;
    return (
      // param change, /users/123 -> /users/456
      r[c].pathname !== u.pathname || // splat param changed, which is not present in match.path
      // e.g. /files/images/avatar.jpg -> files/finances.xls
      ((h = r[c].route.path) == null ? void 0 : h.endsWith("*")) && r[c].params["*"] !== u.params["*"]
    );
  };
  return o === "assets" ? t.filter(
    (u, c) => l(u, c) || s(u, c)
  ) : o === "data" ? t.filter((u, c) => {
    var v;
    let h = n.routes[u.route.id];
    if (!h || !h.hasLoader)
      return !1;
    if (l(u, c) || s(u, c))
      return !0;
    if (u.route.shouldRevalidate) {
      let g = u.route.shouldRevalidate({
        currentUrl: new URL(
          a.pathname + a.search + a.hash,
          window.origin
        ),
        currentParams: ((v = r[0]) == null ? void 0 : v.params) || {},
        nextUrl: new URL(e, window.origin),
        nextParams: u.params,
        defaultShouldRevalidate: !0
      });
      if (typeof g == "boolean")
        return g;
    }
    return !0;
  }) : [];
}
function Bn(e, t, { includeHydrateFallback: r } = {}) {
  return Wn(
    e.map((n) => {
      let a = t.routes[n.route.id];
      if (!a) return [];
      let o = [a.module];
      return a.clientActionModule && (o = o.concat(a.clientActionModule)), a.clientLoaderModule && (o = o.concat(a.clientLoaderModule)), r && a.hydrateFallbackModule && (o = o.concat(a.hydrateFallbackModule)), a.imports && (o = o.concat(a.imports)), o;
    }).flat(1)
  );
}
function Wn(e) {
  return [...new Set(e)];
}
function Vn(e) {
  let t = {}, r = Object.keys(e).sort();
  for (let n of r)
    t[n] = e[n];
  return t;
}
function Kn(e, t) {
  let r = /* @__PURE__ */ new Set();
  return new Set(t), e.reduce((n, a) => {
    let o = JSON.stringify(Vn(a));
    return r.has(o) || (r.add(o), n.push({ key: o, link: a })), n;
  }, []);
}
function Yn(e, t) {
  let r = typeof e == "string" ? new URL(
    e,
    // This can be called during the SSR flow via PrefetchPageLinksImpl so
    // don't assume window is available
    typeof window > "u" ? "server://singlefetch/" : window.location.origin
  ) : e;
  return r.pathname === "/" ? r.pathname = "_root.data" : t && re(r.pathname, t) === "/" ? r.pathname = `${t.replace(/\/$/, "")}/_root.data` : r.pathname = `${r.pathname.replace(/\/$/, "")}.data`, r;
}
function Or() {
  let e = p.useContext(Ce);
  return zt(
    e,
    "You must render this element inside a <DataRouterContext.Provider> element"
  ), e;
}
function Jn() {
  let e = p.useContext(Je);
  return zt(
    e,
    "You must render this element inside a <DataRouterStateContext.Provider> element"
  ), e;
}
var Ot = p.createContext(void 0);
Ot.displayName = "FrameworkContext";
function Ar() {
  let e = p.useContext(Ot);
  return zt(
    e,
    "You must render this element inside a <HydratedRouter> element"
  ), e;
}
function Gn(e, t) {
  let r = p.useContext(Ot), [n, a] = p.useState(!1), [o, l] = p.useState(!1), { onFocus: s, onBlur: u, onMouseEnter: c, onMouseLeave: h, onTouchStart: v } = t, g = p.useRef(null);
  p.useEffect(() => {
    if (e === "render" && l(!0), e === "viewport") {
      let L = (P) => {
        P.forEach((C) => {
          l(C.isIntersecting);
        });
      }, b = new IntersectionObserver(L, { threshold: 0.5 });
      return g.current && b.observe(g.current), () => {
        b.disconnect();
      };
    }
  }, [e]), p.useEffect(() => {
    if (n) {
      let L = setTimeout(() => {
        l(!0);
      }, 100);
      return () => {
        clearTimeout(L);
      };
    }
  }, [n]);
  let y = () => {
    a(!0);
  }, E = () => {
    a(!1), l(!1);
  };
  return r ? e !== "intent" ? [o, g, {}] : [
    o,
    g,
    {
      onFocus: Be(s, y),
      onBlur: Be(u, E),
      onMouseEnter: Be(c, y),
      onMouseLeave: Be(h, E),
      onTouchStart: Be(v, y)
    }
  ] : [!1, g, {}];
}
function Be(e, t) {
  return (r) => {
    e && e(r), r.defaultPrevented || t(r);
  };
}
function Xn({
  page: e,
  ...t
}) {
  let { router: r } = Or(), n = p.useMemo(
    () => pe(r.routes, e, r.basename),
    [r.routes, e, r.basename]
  );
  return n ? /* @__PURE__ */ p.createElement(Qn, { page: e, matches: n, ...t }) : null;
}
function qn(e) {
  let { manifest: t, routeModules: r } = Ar(), [n, a] = p.useState([]);
  return p.useEffect(() => {
    let o = !1;
    return Hn(e, t, r).then(
      (l) => {
        o || a(l);
      }
    ), () => {
      o = !0;
    };
  }, [e, t, r]), n;
}
function Qn({
  page: e,
  matches: t,
  ...r
}) {
  let n = Se(), { manifest: a, routeModules: o } = Ar(), { basename: l } = Or(), { loaderData: s, matches: u } = Jn(), c = p.useMemo(
    () => wr(
      e,
      t,
      u,
      a,
      n,
      "data"
    ),
    [e, t, u, a, n]
  ), h = p.useMemo(
    () => wr(
      e,
      t,
      u,
      a,
      n,
      "assets"
    ),
    [e, t, u, a, n]
  ), v = p.useMemo(() => {
    if (e === n.pathname + n.search + n.hash)
      return [];
    let E = /* @__PURE__ */ new Set(), L = !1;
    if (t.forEach((P) => {
      var T;
      let C = a.routes[P.route.id];
      !C || !C.hasLoader || (!c.some((O) => O.route.id === P.route.id) && P.route.id in s && ((T = o[P.route.id]) != null && T.shouldRevalidate) || C.hasClientLoader ? L = !0 : E.add(P.route.id));
    }), E.size === 0)
      return [];
    let b = Yn(e, l);
    return L && E.size > 0 && b.searchParams.set(
      "_routes",
      t.filter((P) => E.has(P.route.id)).map((P) => P.route.id).join(",")
    ), [b.pathname + b.search];
  }, [
    l,
    s,
    n,
    a,
    c,
    t,
    e,
    o
  ]), g = p.useMemo(
    () => Bn(h, a),
    [h, a]
  ), y = qn(h);
  return /* @__PURE__ */ p.createElement(p.Fragment, null, v.map((E) => /* @__PURE__ */ p.createElement("link", { key: E, rel: "prefetch", as: "fetch", href: E, ...r })), g.map((E) => /* @__PURE__ */ p.createElement("link", { key: E, rel: "modulepreload", href: E, ...r })), y.map(({ key: E, link: L }) => (
    // these don't spread `linkProps` because they are full link descriptors
    // already with their own props
    /* @__PURE__ */ p.createElement("link", { key: E, ...L })
  )));
}
function Zn(...e) {
  return (t) => {
    e.forEach((r) => {
      typeof r == "function" ? r(t) : r != null && (r.current = t);
    });
  };
}
var _r = typeof window < "u" && typeof window.document < "u" && typeof window.document.createElement < "u";
try {
  _r && (window.__reactRouterVersion = "7.5.0");
} catch {
}
function vo(e, t) {
  return ja({
    basename: t == null ? void 0 : t.basename,
    unstable_getContext: t == null ? void 0 : t.unstable_getContext,
    future: t == null ? void 0 : t.future,
    history: sa({ window: t == null ? void 0 : t.window }),
    hydrationData: (t == null ? void 0 : t.hydrationData) || eo(),
    routes: e,
    mapRouteProperties: Sn,
    dataStrategy: t == null ? void 0 : t.dataStrategy,
    patchRoutesOnNavigation: t == null ? void 0 : t.patchRoutesOnNavigation,
    window: t == null ? void 0 : t.window
  }).initialize();
}
function eo() {
  let e = window == null ? void 0 : window.__staticRouterHydrationData;
  return e && e.errors && (e = {
    ...e,
    errors: to(e.errors)
  }), e;
}
function to(e) {
  if (!e) return null;
  let t = Object.entries(e), r = {};
  for (let [n, a] of t)
    if (a && a.__type === "RouteErrorResponse")
      r[n] = new ht(
        a.status,
        a.statusText,
        a.data,
        a.internal === !0
      );
    else if (a && a.__type === "Error") {
      if (a.__subType) {
        let o = window[a.__subType];
        if (typeof o == "function")
          try {
            let l = new o(a.message);
            l.stack = "", r[n] = l;
          } catch {
          }
      }
      if (r[n] == null) {
        let o = new Error(a.message);
        o.stack = "", r[n] = o;
      }
    } else
      r[n] = a;
  return r;
}
var Ir = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i, jr = p.forwardRef(
  function({
    onClick: t,
    discover: r = "render",
    prefetch: n = "none",
    relative: a,
    reloadDocument: o,
    replace: l,
    state: s,
    target: u,
    to: c,
    preventScrollReset: h,
    viewTransition: v,
    ...g
  }, y) {
    let { basename: E } = p.useContext(le), L = typeof c == "string" && Ir.test(c), b, P = !1;
    if (typeof c == "string" && L && (b = c, _r))
      try {
        let _ = new URL(window.location.href), Y = c.startsWith("//") ? new URL(_.protocol + c) : new URL(c), ee = re(Y.pathname, E);
        Y.origin === _.origin && ee != null ? c = ee + Y.search + Y.hash : P = !0;
      } catch {
        K(
          !1,
          `<Link to="${c}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`
        );
      }
    let C = cn(c, { relative: a }), [T, O, f] = Gn(
      n,
      g
    ), U = oo(c, {
      replace: l,
      state: s,
      target: u,
      preventScrollReset: h,
      relative: a,
      viewTransition: v
    });
    function j(_) {
      t && t(_), _.defaultPrevented || U(_);
    }
    let N = (
      // eslint-disable-next-line jsx-a11y/anchor-has-content
      /* @__PURE__ */ p.createElement(
        "a",
        {
          ...g,
          ...f,
          href: b || C,
          onClick: P || o ? t : j,
          ref: Zn(y, O),
          target: u,
          "data-discover": !L && r === "render" ? "true" : void 0
        }
      )
    );
    return T && !L ? /* @__PURE__ */ p.createElement(p.Fragment, null, N, /* @__PURE__ */ p.createElement(Xn, { page: C })) : N;
  }
);
jr.displayName = "Link";
var ro = p.forwardRef(
  function({
    "aria-current": t = "page",
    caseSensitive: r = !1,
    className: n = "",
    end: a = !1,
    style: o,
    to: l,
    viewTransition: s,
    children: u,
    ...c
  }, h) {
    let v = Xe(l, { relative: c.relative }), g = Se(), y = p.useContext(Je), { navigator: E, basename: L } = p.useContext(le), b = y != null && // Conditional usage is OK here because the usage of a data router is static
    // eslint-disable-next-line react-hooks/rules-of-hooks
    co(v) && s === !0, P = E.encodeLocation ? E.encodeLocation(v).pathname : v.pathname, C = g.pathname, T = y && y.navigation && y.navigation.location ? y.navigation.location.pathname : null;
    r || (C = C.toLowerCase(), T = T ? T.toLowerCase() : null, P = P.toLowerCase()), T && L && (T = re(T, L) || T);
    const O = P !== "/" && P.endsWith("/") ? P.length - 1 : P.length;
    let f = C === P || !a && C.startsWith(P) && C.charAt(O) === "/", U = T != null && (T === P || !a && T.startsWith(P) && T.charAt(P.length) === "/"), j = {
      isActive: f,
      isPending: U,
      isTransitioning: b
    }, N = f ? t : void 0, _;
    typeof n == "function" ? _ = n(j) : _ = [
      n,
      f ? "active" : null,
      U ? "pending" : null,
      b ? "transitioning" : null
    ].filter(Boolean).join(" ");
    let Y = typeof o == "function" ? o(j) : o;
    return /* @__PURE__ */ p.createElement(
      jr,
      {
        ...c,
        "aria-current": N,
        className: _,
        ref: h,
        style: Y,
        to: l,
        viewTransition: s
      },
      typeof u == "function" ? u(j) : u
    );
  }
);
ro.displayName = "NavLink";
var ao = p.forwardRef(
  ({
    discover: e = "render",
    fetcherKey: t,
    navigate: r,
    reloadDocument: n,
    replace: a,
    state: o,
    method: l = ut,
    action: s,
    onSubmit: u,
    relative: c,
    preventScrollReset: h,
    viewTransition: v,
    ...g
  }, y) => {
    let E = so(), L = uo(s, { relative: c }), b = l.toLowerCase() === "get" ? "get" : "post", P = typeof s == "string" && Ir.test(s), C = (T) => {
      if (u && u(T), T.defaultPrevented) return;
      T.preventDefault();
      let O = T.nativeEvent.submitter, f = (O == null ? void 0 : O.getAttribute("formmethod")) || l;
      E(O || T.currentTarget, {
        fetcherKey: t,
        method: f,
        navigate: r,
        replace: a,
        state: o,
        relative: c,
        preventScrollReset: h,
        viewTransition: v
      });
    };
    return /* @__PURE__ */ p.createElement(
      "form",
      {
        ref: y,
        method: b,
        action: L,
        onSubmit: n ? u : C,
        ...g,
        "data-discover": !P && e === "render" ? "true" : void 0
      }
    );
  }
);
ao.displayName = "Form";
function no(e) {
  return `${e} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`;
}
function Hr(e) {
  let t = p.useContext(Ce);
  return z(t, no(e)), t;
}
function oo(e, {
  target: t,
  replace: r,
  state: n,
  preventScrollReset: a,
  relative: o,
  viewTransition: l
} = {}) {
  let s = dn(), u = Se(), c = Xe(e, { relative: o });
  return p.useCallback(
    (h) => {
      if (zn(h, t)) {
        h.preventDefault();
        let v = r !== void 0 ? r : ve(u) === ve(c);
        s(e, {
          replace: v,
          state: n,
          preventScrollReset: a,
          relative: o,
          viewTransition: l
        });
      }
    },
    [
      u,
      s,
      c,
      r,
      n,
      t,
      e,
      a,
      o,
      l
    ]
  );
}
var io = 0, lo = () => `__${String(++io)}__`;
function so() {
  let { router: e } = Hr(
    "useSubmit"
    /* UseSubmit */
  ), { basename: t } = p.useContext(le), r = Pn();
  return p.useCallback(
    async (n, a = {}) => {
      let { action: o, method: l, encType: s, formData: u, body: c } = _n(
        n,
        t
      );
      if (a.navigate === !1) {
        let h = a.fetcherKey || lo();
        await e.fetch(h, r, a.action || o, {
          preventScrollReset: a.preventScrollReset,
          formData: u,
          body: c,
          formMethod: a.method || l,
          formEncType: a.encType || s,
          flushSync: a.flushSync
        });
      } else
        await e.navigate(a.action || o, {
          preventScrollReset: a.preventScrollReset,
          formData: u,
          body: c,
          formMethod: a.method || l,
          formEncType: a.encType || s,
          replace: a.replace,
          state: a.state,
          fromRouteId: r,
          flushSync: a.flushSync,
          viewTransition: a.viewTransition
        });
    },
    [e, t, r]
  );
}
function uo(e, { relative: t } = {}) {
  let { basename: r } = p.useContext(le), n = p.useContext(se);
  z(n, "useFormAction must be used inside a RouteContext");
  let [a] = n.matches.slice(-1), o = { ...Xe(e || ".", { relative: t }) }, l = Se();
  if (e == null) {
    o.search = l.search;
    let s = new URLSearchParams(o.search), u = s.getAll("index");
    if (u.some((h) => h === "")) {
      s.delete("index"), u.filter((v) => v).forEach((v) => s.append("index", v));
      let h = s.toString();
      o.search = h ? `?${h}` : "";
    }
  }
  return (!e || e === ".") && a.route.index && (o.search = o.search ? o.search.replace(/^\?/, "?index&") : "?index"), r !== "/" && (o.pathname = o.pathname === "/" ? r : ie([r, o.pathname])), ve(o);
}
function co(e, t = {}) {
  let r = p.useContext($t);
  z(
    r != null,
    "`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?"
  );
  let { basename: n } = Hr(
    "useViewTransitionState"
    /* useViewTransitionState */
  ), a = Xe(e, { relative: t.relative });
  if (!r.isTransitioning)
    return !1;
  let o = re(r.currentLocation.pathname, n) || r.currentLocation.pathname, l = re(r.nextLocation.pathname, n) || r.nextLocation.pathname;
  return ft(a.pathname, l) != null || ft(a.pathname, o) != null;
}
new TextEncoder();
export {
  Ce as DataRouterContext,
  Je as DataRouterStateContext,
  ht as ErrorResponseImpl,
  kr as FetchersContext,
  ao as Form,
  Ot as FrameworkContext,
  je as IDLE_BLOCKER,
  _a as IDLE_FETCHER,
  Et as IDLE_NAVIGATION,
  jr as Link,
  mt as LocationContext,
  ro as NavLink,
  le as NavigationContext,
  po as Outlet,
  Xn as PrefetchPageLinks,
  se as RouteContext,
  Fn as Router,
  mo as RouterProvider,
  $t as ViewTransitionContext,
  sa as createBrowserHistory,
  vo as createBrowserRouter,
  ve as createPath,
  ja as createRouter,
  z as invariant,
  Ye as isRouteErrorResponse,
  Sn as mapRouteProperties,
  ft as matchPath,
  pe as matchRoutes,
  ye as parsePath,
  Ma as resolvePath,
  Qt as unstable_RouterContextProvider,
  uo as useFormAction,
  cn as useHref,
  Ge as useInRouterContext,
  oo as useLinkClickHandler,
  Se as useLocation,
  dn as useNavigate,
  mn as useOutlet,
  Xe as useResolvedPath,
  Ln as useRouteError,
  so as useSubmit,
  co as useViewTransitionState
};
